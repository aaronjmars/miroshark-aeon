*Push Recap — 2026-06-15*
aaronjmars/MiroShark — REFACTORING: round-2 cleanup dominates, one community deploy fix ships.

Shipped to users:
• #159 — frontend API base now defaults to same-origin `/api` (was hardcoded localhost:5001), so the UI works behind LAN hosts + reverse proxies; API errors now surface a readable message. First-time contributor dan-and. Neo4j bumped 5.15→5.26.

Under the hood:
• #163/#164 — round-2 hygiene: one shared `safe_load_json` reader replaces 11 copy-pasted copies, shared time/belief/base-url helpers hoisted, dead imports dropped. #165 fixes 2 tests that needed an LLM key in CI. Net source change is subtractive — engine (simulation_runner/manager) untouched for the 3rd straight window.

Shape: 1 user-visible · 3 internal · 0 infra · 25 bot-filtered · 4 merged PRs
Volume: ~1.5k+/567− (≈1.2k of that is docs/cleanup audit reports)

Full recap: https://github.com/aaronjmars/miroshark-aeon/blob/main/articles/push-recap-2026-06-15.md
