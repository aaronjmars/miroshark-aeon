# Push Recap — 2026-06-09

## Overview
2 substantive merges in the last 24 hours, both Aeon-authored: PR #153 on MiroShark ships `/api/activity.json` (the "what just completed?" polling feed) and PR #55 on miroshark-aeon encodes the May-31 noise-exclusion convention as a hard rule inside `push-recap` itself. The MiroShark surface catalog moves 34 → 35 and the 43-PR zero-deps streak continues; the aeon repo eliminates a 7-day re-derivation burden on this very skill — the recap you're reading is the first one to run with that filter baked into the prompt instead of re-discovered from log precedent.

**Stats:** 16 files changed, +1,778 / −17 lines across 2 PR-squash commits. 30 cron auto-commits on the aeon repo were excluded as noise per the May-31 convention (now formalized as step 5 of the push-recap skill, courtesy of one of the two PRs in this window).

---

## aaronjmars/MiroShark

### Theme: The lightweight polling primitive — `/api/activity.json` lands as the 35th surface

**Summary:** The "what just completed?" question integrators ask between gallery refreshes has had no first-class answer until now. The existing primitives all answer adjacent questions: `/api/simulation/public` is a fully filterable gallery (heavy for polling), `/api/feed.rss` and `/api/feed.atom` are subscription-shaped (XML envelope, not optimised for JSON pollers), and `/api/status.json` is a liveness probe (one entry, no payload). Activity feed fills the gap — N most recent public+completed sims, reverse-chronological, JSON, polling-cache-friendly, keyless.

**Commits:**
- `97e39f8` — `feat: add /api/activity.json — what-just-completed polling feed (#153)`
  - New file `backend/app/api/activity.py` (+144 lines): registers `activity_bp` with one route `@activity_bp.route("/activity.json", methods=["GET"])`. The route module-docstring places the surface explicitly as a sibling of `stats.py` / `status.py` / `surfaces.py` ("every blueprint in this small family describes the platform itself rather than one simulation") and locks the response envelope (`schema_version`, `count`, `results[]` with `sim_id`/`scenario_title`/`direction`/`confidence_pct`/`quality_health`/`total_rounds`/`completed_at`/`project_id`). `Cache-Control: public, max-age=30` matches the cadence external polling loops actually tick at; an ETag derived from `count + newest completed_at` short-circuits a conditional GET to 304 without re-serialising.
  - New file `backend/app/services/activity_feed.py` (+439 lines): the substance — `build_activity_feed(sim_root, *, limit)`, `clamp_limit(value)`, `feed_etag(payload)`, and `SCENARIO_TITLE_MAX_CHARS = 100`. Reuses `signal_service.compute_signal` so `direction`/`confidence_pct`/`quality_health` match the per-sim signal.json byte-for-byte, and mirrors `batch_status._final_belief_from_trajectory` so `total_rounds` matches `BatchStatusEntry.total_rounds` byte-for-byte. That choice is what lets a consumer correlate this feed with the existing batch-status and signal endpoints without having to reconcile drift.
  - Modified `backend/app/__init__.py` (+19, −1): registers the blueprint at `/api` (no sub-prefix, so the URL stays the short well-known polling path) and adds `/api/activity.json` to the `internal_auth_guard` allow-list. The inline comment block explicitly notes this is the same posture as `/api/status.json` and `/api/simulation/batch-status` — three keyless platform-level endpoints now in a contiguous block of the guard.
  - Modified `backend/app/api/__init__.py` (+9): exports `activity_bp`.
  - Modified `backend/app/services/surfaces_catalog.py` (+9): a 35th catalogued surface, `type: discovery`. This is the discovery family's third entry (alongside `feed_atom` and `feed_rss`) — first non-XML discovery surface.
  - Modified `backend/openapi.yaml` (+248): `/api/activity.json` path entry plus `ActivityFeed` + `ActivityFeedEntry` schemas with the field set locked. The OpenAPI spec is large for what looks like a small endpoint — that's because both the envelope schema and the entry schema are described in full (every nullability, every enum, every range), so a client generator can produce a fully-typed binding without further inference.
  - Modified `backend/tests/test_unit_openapi.py` (+3): teaches the `test_documented_paths_exist_in_flask` drift-test about `activity_bp`'s prefix. This is a single-line gotcha that surfaced in a follow-up commit during the PR — the drift-test statically rebuilds the Flask route set from `_BLUEPRINT_PREFIXES` rather than running the app, so `activity_bp` had to be added to that map for `@activity_bp.route("/activity.json")` to resolve correctly during the test. Without this, the documented spec path looked like a phantom and the drift test failed.
  - New file `backend/tests/test_unit_activity_feed.py` (+694): 39 offline unit tests across 18 properties. Notable coverage areas: publish gate (private + in-flight sims never appear), ordering determinism (desc by `completed_at`, ties broken by `sim_id` desc), `?limit=` clamping behaviour (default 20, low → 1, high → 50, booleans rejected, non-numeric → default), scenario truncation (100 chars + ellipsis), signal-pipeline equivalence (direction derivation matches signal.json byte-for-byte), `total_rounds` equivalence with batch-status, degraded-trajectory tolerance (completed sim with no trajectory still appears with null analytics — defensive against partial data), `updated_at` → `created_at` fallback, corrupt-state-json skip, dotfile skip, ETag format and change detection, JSON-serialisability, catalog discoverability, OpenAPI spec coverage, blueprint registration, auth-guard allow-list, and route-file drift guards.
  - Modified `frontend/src/api/simulation.js` (+49): `getActivityFeedUrl(limit?, origin?)` + `getActivityFeed(limit?)` helpers in the same shape as the existing `getPlatformStatus` and `batchSimulationStatus` helpers. The frontend doesn't consume the feed yet — the helpers are wired so an upcoming dashboard component can drop in without API plumbing work.
  - Modified `docs/API.md` (+1): platform-section table row with deltas vs `/api/simulation/public`, `/api/feed.rss`, `/api/status.json`. One-line — the OpenAPI spec carries the substance.

**Impact:** This is the polling-loop primitive integrators have been asking the existing gallery and feed endpoints to be. Aeon's own `push-recap` (this skill) is on the explicit consumer list in the route docstring; so are Capacitr / AntFleet polling loops, status dashboards, and social bots. The endpoint also closes a small consistency gap that the recently-shipped batch-status created — until today, a consumer wanting "most recent completed sims" had to either page through `/api/simulation/public` (filterable but unindexed by completion) or parse the RSS/Atom envelopes; the cross-surface `total_rounds` semantics now line up across `signal.json`, `batch-status`, and `activity.json`. Catalog 34 → 35; zero new deps (43-PR streak intact).

---

## aaronjmars/miroshark-aeon

### Theme: A 7-day re-derivation, retired

**Summary:** Every push-recap from Jun-01 through Jun-07 spent part of its budget re-discovering the same rule: `aeonframework` commits prefixed `chore(scheduler):`, `chore(cron):`, or `chore(<skill>): auto-commit` on the agent repo are pipeline churn, not substantive shipping events. The rule lived in operator memory and log precedent (the May-31 convention) but not in the skill prompt — so each fresh run had to re-derive it from first principles. PR #55 makes the rule explicit and mechanical, inside the prompt.

**Commits:**
- `4eb9efb` — `improve: encode push-recap agent-repo noise-exclusion as step 5 (#55)`
  - Modified `skills/push-recap/SKILL.md` (+21, −6): inserts a new **Step 5 — Filter agent-repo cron noise** between dedup (step 4) and diff-reading (now step 6). The filter applies *only* to the agent repo (this repo, or any whose name ends in `-aeon`) — never to watched project repos, where every commit is potentially substantive. A commit is dropped if both: (a) author/actor is `aeonframework`, AND (b) the first line of the commit message matches one of three exact prefixes — `chore(scheduler):` (cron-state churn), `chore(cron):` (per-skill success markers), or `chore(<any-skill-name>): auto-commit` (skill output payloads landing on main via the auto-commit pipeline, already covered in the skill's own section in today's log so recapping again would duplicate). Genuinely substantive `aeonframework` commits (interactive PR merges, manual fixes, non-pipeline content commits) stay in the recap because their message prefixes don't match. Steps 5–10 renumbered to 6–11. The body of the new step explicitly cites the seven days of re-derivation that prompted the encoding.
  - Modified `memory/MEMORY.md` (+2): a Skills Built table row and a Lessons Learned row referencing this PR.
  - Modified `memory/logs/2026-06-08.md` (+25): the self-improve skill's own log entry from when it generated the PR — it records the trigger (seven daily re-derivations), the considered alternatives (move to `CLAUDE.md`, separate `memory/topics/cron-noise-patterns.md` registry, widen the filter to drop all `aeonframework` commits), and why each was rejected.
  - Modified `.outputs/self-improve.md` (+8, −10): refreshed skill-output snapshot in the standard auto-commit path.
  - New file `dashboard/outputs/self-improve-2026-06-08T13-51-38Z.json` (+106): json-render dashboard spec for the self-improve run.
  - Modified `memory/token-usage.csv` (+1): one CSV row appended.

**Impact:** First-order: future `push-recap` runs (this one is the first) skip the re-derivation entirely and short-circuit the expensive `gh api .../commits/<sha>` fan-out for filtered noise. Second-order: pairs with the existing step-3 `PUSH_RECAP_QUIET` short-circuit — on collapsed-to-zero days the filter runs *before* diff-reading, so quiet days now cost almost nothing. Third-order: the encoded filter eliminates a small but real risk — that a cron auto-commit gets misclassified as a substantive shipping event by a recap run that happens to lose context. This is the second prompt-level Aeon self-improvement in 4 days (PR #53 encoded the feature-skill auth-posture step on Jun-06, paid off on PR #150 the same window); the pattern of "re-derivation observed N consecutive days → encode as explicit prompt step" is becoming a load-bearing maintenance loop for the agent.

---

## Developer Notes

- **New dependencies:** Zero. The 43-PR zero-deps streak on MiroShark holds — every PR since #110 (and counting) has shipped without adding a Python or JS dependency. Activity feed reuses `signal_service.compute_signal` and the trajectory-walking pattern from `batch_status`; the only new "thing" in scope is `SCENARIO_TITLE_MAX_CHARS = 100`.
- **Breaking changes:** None. The new MiroShark endpoint is purely additive; the auth-guard change is an allow-list addition, not a posture change for any existing route. The push-recap step renumbering is internal to the skill prompt — no consumer depends on the step numbers.
- **Architecture shifts:** `/api/activity.json` is the third entry in the "platform-shaped, keyless" cluster — `/api/status.json` (liveness probe), `/api/simulation/batch-status` (parallel polling), and now activity feed. All three share the same posture inside `internal_auth_guard` (allow-list-by-path), all three reuse the same per-sim publish gate inside their service modules as the actual privacy boundary. That's now a coherent enough pattern that a new platform-shaped keyless surface has a working template.
- **Tech debt:** The `test_documented_paths_exist_in_flask` drift-test had a quiet failure mode — a blueprint not registered in its static `_BLUEPRINT_PREFIXES` map silently resolved to None and was skipped, so a documented spec path could look like a phantom even though the route existed. The follow-up commit on PR #153 fixed the symptom (added `activity_bp` to the map); a future improvement would be to derive the map from blueprint registration in `app/__init__.py` rather than maintaining it by hand.

## What's Next

- The MiroShark catalog is at 35 entries. From the Jun-08 repo-actions batch, idea #1 (Simulation Activity Feed) is now landed; ideas #2 (Trending Simulation Topics), #3 (MCP Tool Catalog JSON), #4 (Pre-Run Cost Estimator), and #5 (Chinese README) are unbuilt. Idea #5 carries hyperstition weight — Chinese-locale contributor or coverage by Jun-15 (deadline 6 days out).
- From the Jun-06 batch, two ideas still unbuilt: #2 (Simulation Payload Validator), #4 (Monthly Stats Time-Series), and #5 (Platform Agent Behavior Census).
- Branches created but not merged: no open Aeon PRs on MiroShark at recap close (PR #153 merged 12:03 UTC). On miroshark-aeon, PR #55 merged 11:35 UTC — both repos are at 0 open Aeon PRs again, the third time this has happened in the recap series (Jun-07, Jun-08, Jun-09 all closed at 0/0).
- The prompt-level self-improvement loop has two more triggers worth watching: (1) the `test_documented_paths_exist_in_flask` blueprint-map maintenance burden is small but real, and (2) the agent-repo article writing flow doesn't yet have a comparable "noise filter" for the day's own log entries when computing what's surprising.
