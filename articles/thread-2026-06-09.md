# Thread — 2026-06-09

**Topic:** Simulation Activity Feed — GET /api/activity.json (PR #153)
**Score:** 6 (signals: new feature shipped — PR opened on watched repo)
**Source events:**
- Feature Build — Simulation Activity Feed: `GET /api/activity.json[?limit=N]` merged 2026-06-09T12:03:31Z; public/keyless; 35th catalog surface (type: discovery, third member after feed_atom/feed_rss); third "platform-shaped, keyless" surface alongside /api/status.json + /api/simulation/batch-status; direction/confidence_pct/quality_health byte-for-byte from signal.json's compute_signal; total_rounds byte-for-byte from batch-status's derivation; 39 offline unit tests; 43rd consecutive zero-deps PR; 9 files, +1,612 lines; named integrators: Capacitr, AntFleet, Aeon push-recap skills
- Repo Article: "The Polling Loop Now Has an Endpoint to Ask 'Anything Yet?'" — frames trio completion and cross-surface byte-equivalence property

---

## Tweet 1
Three keyless endpoints. Three polling questions. /api/status.json: is the platform running? /api/simulation/batch-status: what's the state of these sims? /api/activity.json, merged today as PR #153: what just completed? The polling loop is now fully wired.

## Tweet 2
Before today, a keyless polling loop could confirm the platform was running (/api/status.json) and check a batch of known sim IDs (/api/simulation/batch-status). There was no endpoint to ask what had completed recently. Discovery required auth.

## Tweet 3
GET /api/activity.json returns the most-recent public completed sims, reverse-chronological (?limit= clamped 1–50, default 20). direction/confidence_pct/quality_health match signal.json byte-for-byte — same compute path. 30s public cache, ETag short-circuit.

## Tweet 4
Capacitr, AntFleet, and Aeon's push-recap skills are named in the PR as consumers. Each runs a polling loop against MiroShark surfaces. Now all three have an endpoint to ask what completed — without knowing sim IDs in advance, without authenticating.

## Tweet 5
35th catalogued surface, 43rd consecutive zero-dependency PR. The third discovery surface — alongside feed_atom and feed_rss — but the first shaped for machine polling, not subscription readers. https://github.com/aaronjmars/MiroShark/pull/153

---

**Character counts:** 1: 257 | 2: 244 | 3: 258 | 4: 250 | 5: 243
**Artifact link:** https://github.com/aaronjmars/MiroShark/pull/153
