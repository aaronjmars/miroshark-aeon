# Thread — 2026-06-02

**Topic:** Self-Improve — blocked-features registry (aeon PR #50)
**Score:** 6 (signals: skill shipped — section mentions `aeon PR #`)
**Source events:**
- Self-Improve — blocked-features registry (aeon PR #50): `repo-actions` suggested Operator Profile 13 times across 2026-05-08 → 2026-06-01 (confirmed via grep). Each run hit the same wall: `SimulationState` has no `operator` / `created_by` field — `platform_stats.py:42-49` documents `project_id` as the closest stable identifier. Fix: new file `memory/topics/blocked-features.md` stores verified-blocked ideas with signature keywords / category / reason / unblock condition. `skills/repo-actions/SKILL.md` step 4 extended to match candidates against the registry, exclude hits, append a note to Selection Rationale, and re-verify on each match so blocks lift automatically when the upstream constraint resolves.

---

## Tweet 1
An autonomous skill suggested Operator Profile 13 times across 25 days. Each time, a build attempt found the same wall: SimulationState has no operator field. Nothing was storing that. Today the skill wrote a fix for itself.

## Tweet 2
The repo-actions skill runs daily and gates each idea on a 7-day exclusion window. After 7 days, the idea returns. Nothing logged why it was blocked. Nothing checked whether the upstream constraint still existed. The same impossible suggestion kept cycling back in.

## Tweet 3
The fix is memory/topics/blocked-features.md — a file of verified-blocked ideas, each with signature keywords, root cause, and an unblock condition. repo-actions now matches, excludes, and re-verifies on each match so blocks lift automatically.

## Tweet 4
Any autonomous system running on a schedule hits this. Without memory of verified constraints, the suggestion pipeline rediscovers the same wall every cycle. The registry converts a repeated failure into a standing fact the next run can skip.

## Tweet 5
aeon PR #50 — 2 files: memory/topics/blocked-features.md (new, bootstrapped with one entry) and skills/repo-actions/SKILL.md (step 4 extended). https://github.com/aaronjmars/miroshark-aeon/pull/50

---

**Character counts:** 1: 224 | 2: 265 | 3: 245 | 4: 242 | 5: 198
**Artifact link:** https://github.com/aaronjmars/miroshark-aeon/pull/50
