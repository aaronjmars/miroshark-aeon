# Thread — 2026-06-12

**Topic:** MiroShark surface catalog type filter — PR #157 merged
**Score:** 6 (signals: new-feature-pr-on-watched-repo)
**Source events:**
- feature-2026-06-12: PR #157 adds `?type=` server-side filter to `GET /api/surfaces.json`; previously client-side only (jq recipe in FEATURES.md); 8 new unit tests; backward-compatible
- push-recap-2026-06-12: PR #157 merged; verdict SHIPPING — /api/surfaces.json gains a ?type= category filter for integrators

---

## Tweet 1
docs/FEATURES.md said GET /api/surfaces.json supported ?type= filtering. the code didn't. PR #157 closed the gap — filter is server-side now, integrators no longer pull the full catalog to narrow it.

## Tweet 2
The jq recipe was in the docs. the server-side path wasn't. integrators polling a single surface type had to download everything and filter locally. that's a bandwidth tax per poll and no cache leverage per category.

## Tweet 3
PR #157: surfaces_catalog.py now accepts a type param and filters server-side before returning. no-type paths unchanged. 8 new tests. openapi.yaml updated. the feature was in the docs — now it's also in the code.

## Tweet 4
three integrators are building on MiroShark as AI infrastructure: RevaultDrops, AntFleet, Capacitr. each has different simulation types. server-side filtering means they poll exactly what they need. no local filter layer. the API gets cheaper to integrate.

## Tweet 5
surface catalog type filter — merged, live. diff and test coverage at https://github.com/aaronjmars/MiroShark/pull/157

---

**Character counts:** 1: 199 | 2: 216 | 3: 212 | 4: 256 | 5: 118
**Artifact link:** https://github.com/aaronjmars/MiroShark/pull/157
