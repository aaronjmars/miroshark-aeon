# Thread — 2026-06-08

**Topic:** Signed Simulation Result — GET /api/simulation/:id/signed-result.json (PR #152)
**Score:** 6 (signals: new feature shipped — PR opened on watched repo)
**Source events:**
- Feature Built — Signed Simulation Result: `GET /api/simulation/:id/signed-result.json` merged 2026-06-08T12:40:12Z; HMAC-SHA256 envelope over canonical JSON signal payload (`sort_keys=True, separators=(",",":"), ensure_ascii=True`); signed_at excluded from signed material so identical sig across two calls on same finished sim; empty WEBHOOK_SECRET returns `signed=false` (no error); 25 offline unit tests; catalog 33→34; 42nd consecutive zero-deps PR; 9 files, +1,051 lines; Capacitr and AntFleet named in PR body as immediate consumers
- Repo Article: "The 34th Surface Is the First One That Travels" — frames as first cryptographic-verifiability surface; HMAC-vs-Ed25519 design rationale; swap-seam via schema_version + algorithm fields

---

## Tweet 1
PR #152 ships GET /api/simulation/:id/signed-result.json. Every finished sim now carries an HMAC-SHA256 signature over its canonical signal payload. You can verify the output offline, without re-fetching, without staying authenticated, without trusting the connection.

## Tweet 2
Before today, trust in a MiroShark sim output was connection-level. signal.json is auth-gated — the credentials that got you in were also your proof the data was authentic. Cache the payload and go offline: no verification path existed.

## Tweet 3
HMAC-SHA256 over canonical JSON: sorted keys, ASCII-only, comma-colon separators. The signed_at timestamp is outside the signed block — two calls on the same finished sim return identical signatures, different timestamps. 25 tests, one pinning the exact canonical form.

## Tweet 4
Capacitr and AntFleet are named in the PR as the immediate consumers. Both run operations on simulation outputs — settlement ledgers and leaderboard entries — where they need to prove the data came from MiroShark unchanged. The trust anchor is now the artifact, not the session.

## Tweet 5
34th catalogued surface on MiroShark. 42nd consecutive zero-dependency PR. schema_version and algorithm fields are the swap-seam for a future Ed25519 upgrade. https://github.com/aaronjmars/MiroShark/pull/152

---

**Character counts:** 1: 268 | 2: 236 | 3: 269 | 4: 278 | 5: 207
**Artifact link:** https://github.com/aaronjmars/MiroShark/pull/152
