# Thread — 2026-06-04

**Topic:** Per-Project Simulation Statistics — /api/project/<id>/stats (PR #147)
**Score:** 6 (signals: new feature shipped — PR merged on watched repo)
**Source events:**
- Feature Build — Per-Project Simulation Statistics: `GET /api/project/<project_id>/stats` per-project sibling of `/api/stats`; new `quality_distribution` field (excellent/good/fair/poor); 28 offline tests; surfaces catalog 30→31; 39th zero-deps PR; +1,864/-10 across 13 files; merged 2026-06-04T15:16:03Z
- Push Recap: PR #147 merged today (Aeon-built); `project_stats_bp` is the second blueprint on `stats.py` (three surfaces on one file, was two yesterday)

---

## Tweet 1
MiroShark's API had per-platform stats and per-simulation stats. It had nothing in between. PR #147 ships the missing layer: GET /api/project/<id>/stats aggregates across every sim under a single project ID.

## Tweet 2
The platform exposed two endpoints: /api/stats for the whole instance and 26 per-simulation surfaces for individual runs. Operators running multiple named projects had to fetch the platform aggregate and filter it themselves on the client side.

## Tweet 3
The new route validates project_id at the boundary, returns an all-zero envelope for unknown IDs (not a 404), caches at 60s, and adds one new field absent from the platform aggregate: quality_distribution — excellent/good/fair/poor buckets, only useful at per-project granularity.

## Tweet 4
This establishes a third axis the stats API can grow along. Whole-instance / per-project / per-sim is how multi-tenant analytics APIs end up shaped. stats.py now mounts three blueprints where it mounted two yesterday. 39th consecutive PR with zero new dependencies.

## Tweet 5
PR #147 — 13 files, +1,864 lines, 28 offline tests, zero new dependencies. https://github.com/aaronjmars/MiroShark/pull/147

---

**Character counts:** 1: 208 | 2: 244 | 3: 280 | 4: 265 | 5: 123
**Artifact link:** https://github.com/aaronjmars/MiroShark/pull/147
