# Thread — 2026-06-06

**Topic:** Multi-Sim Batch Status Lookup — POST /api/simulation/batch-status (PR #150)
**Score:** 6 (signals: new feature shipped — PR opened on watched repo)
**Source events:**
- Feature Built — Multi-Sim Batch Status Lookup: `POST /api/simulation/batch-status` with body `{"sim_ids": [...]}` (1–20 ids); first batch-shape primitive in the surface catalog (all 31 prior surfaces were one-or-all); privacy invariant: private + unknown IDs emit byte-identical `{found: false, ...nulls}` envelope; 26 offline tests; catalog 31→32; 41st consecutive zero-deps PR; PR #150 opened 2026-06-06T11:34:38Z, still open at write
- Token Report: +15.24% 24h (first bounce after 8 consecutive lower closes); scored separately, lower than the feature

---

## Tweet 1
MiroShark had 31 API surfaces before today. All 31 ask about one sim or about all sims. PR #150 ships the first that takes a list: POST /api/simulation/batch-status, 1 to 20 IDs per request.

## Tweet 2
The per-sim surfaces answer questions about one run: signal, agents, sparklines, volatility, transcript. Platform surfaces answer questions about the whole instance: stats, catalog, status probe. Nothing in between for callers tracking a set of sims.

## Tweet 3
Takes a JSON body of up to 20 sim IDs, returns one entry per ID in input order, duplicates honored. Private and unknown IDs share an identical envelope — callers cannot probe whether a private sim exists. 26 tests, one dedicated to that invariant.

## Tweet 4
AntFleet's miroshark-bench, Capacitr's polling loop, the integrators in ECOSYSTEM.md — all of them need to monitor a set of sims at once. One batch call replaces N sequential calls. This is the primitive that makes polling on a fleet of sims practical.

## Tweet 5
PR #150 — 10 files, 26 offline tests, 41st consecutive zero-dependency PR on MiroShark. https://github.com/aaronjmars/MiroShark/pull/150

---

**Character counts:** 1: 190 | 2: 250 | 3: 247 | 4: 252 | 5: 136
**Artifact link:** https://github.com/aaronjmars/MiroShark/pull/150
