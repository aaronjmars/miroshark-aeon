# Push Recap — 2026-06-08

## Overview
Three MiroShark merges and two miroshark-aeon merges across the recap window — every single one Aeon-built, every single one zero new deps. The MiroShark side closed out the entire 2026-06-06 repo-actions batch's three deliverables (`/api/stats/distribution.json` PR #151, `POST /api/simulation/batch-status` PR #150, signed `signal.json` PR #152) and pushed the surfaces catalog from 31 → 34 in ~21 hours of wall-clock. The miroshark-aeon side shipped two skill-prompt self-improvements: PR #53 made the `feature` skill decide auth posture before writing code, and PR #54 enriched `repo-pulse` with one-line GitHub profile summaries for every new stargazer and forker (with a low-signal fake-star flag). Five PRs by Aaron + Aeon, two repos, all on the back of the same pattern: encode each near-miss as a step in the skill that hit it.

**Stats:** 34 files changed, +4,638 / -22 lines across 5 PR-squash commits. Zero new dependencies on either side — MiroShark hits **42 PRs in a row** with no `requirements.txt`/`package.json` additions.

---

## aaronjmars/MiroShark

### Theme 1: The Jun-06 repo-actions batch ships in full — catalog 31 → 34 inside ~21 hours
**Summary:** The 2026-06-06 repo-actions skill picked five candidate ideas; three of them turned into merged PRs inside this recap window. Two were analytics surfaces (outcome-distribution PR #151, batch-status PR #150) that landed 8m10s apart on Jun 7 afternoon; the third was a security primitive (signed signal payloads PR #152) that landed the next morning. Each one fits a different consumer profile — researchers + threshold-tuners for distribution.json, parallel-pollers (AntFleet/Capacitr) for batch-status, offline-verifying archivists for signed-result. The clean property: all three reuse `signal_service.compute_signal` as the source of truth, so a sim's stance and confidence is byte-identical across every surface that surfaces it.

**Commits:**

- `7075897` — `feat: GET /api/stats/distribution.json — platform-wide outcome distribution (#151)` — merged 2026-06-07T15:41:37Z by Aaron
  - **New** `backend/app/services/outcome_distribution.py` (+533 lines, pure stdlib `os`+`json`+`time`+`threading`+`datetime`) — `build_distribution(sim_root)` walks completed-and-public sims, bucket-counts four axes (`by_direction` bullish/neutral/bearish, `by_confidence` ≥70/40-70/<40, `by_quality` excellent/good/fair/poor, `by_round_count` short<10/medium 10-20/long>20), emits two scalar averages (`avg_confidence_pct`, `avg_total_rounds`), wraps with a 300-second TTL cache + ETag derived from `total_analyzed + YYYY-MM` prefix of `newest_completed_at`
  - **Modified** `backend/app/api/stats.py` (+65) — `stats_bp.route('/distribution.json')` handler, shares `internal_auth_guard` posture with `/api/stats`, identical 304/ETag/`Cache-Control: max-age=300` semantics
  - **Modified** `backend/app/services/surfaces_catalog.py` (+9) — `outcome_distribution` catalog entry; catalog 32 → 33
  - **Modified** `backend/openapi.yaml` (+398) — `/api/stats/distribution.json` path entry + full `OutcomeDistribution` schema with per-bucket field shapes
  - **New** `backend/tests/test_unit_outcome_distribution.py` (+810) — 30 offline tests: empty/missing/blank `sim_root`, publish-gate (private + incomplete + case-insensitive status), direction plurality + tie-break, confidence tier boundaries, quality case-insensitive + unknown-value exclusion, round-count thresholds, missing-trajectory tolerance, pct-sum identities, average rounding, ISO-8601 timestamps, JSON serialisability, 5-min cache TTL + `force_refresh`, ETag derivation + distinctness from platform/project siblings, route/catalog/openapi/frontend drift guards
  - **Modified** `docs/API.md` (+1) and `docs/FEATURES.md` (+58) — table row + dedicated "Platform Outcome Distribution" section with example envelope and a four-audience framing (researchers / Aeon digest skills / directory builders / threshold-calibrating integrators)
  - **Modified** `frontend/src/api/simulation.js` (+36) — `getOutcomeDistribution()` + `getOutcomeDistributionUrl()` helpers

- `6b628a6` — `feat: POST /api/simulation/batch-status — multi-sim status lookup (#150)` — merged 2026-06-07T15:49:47Z by Aaron
  - **New** `backend/app/services/batch_status.py` (+394 lines, pure stdlib `os`+`json`+`re`) — `MAX_BATCH_SIZE = 20`, `is_valid_sim_id()` enforces `^[A-Za-z0-9_\-\.]{1,128}$` so path-traversal never reaches the disk read, `build_batch_status()` iterates input order preserving duplicates and reuses `signal_service.compute_signal` so the per-sim analytics fields are byte-identical to the per-sim `signal.json`
  - **Modified** `backend/app/api/simulation.py` (+128) — `simulation_bp.route('/batch-status', methods=['POST'])` handler with explicit 400 paths for non-JSON-object body, non-array `sim_ids`, empty list, over-cap list, and per-id validation failure; 500 only on `build_batch_status` exception; `Cache-Control: no-store` baked in
  - **Modified** `backend/app/__init__.py` (+9) — adds `/api/simulation/batch-status` to the `internal_auth_guard` allow-list alongside `/api/status.json` (the second `/api/*` endpoint deliberately auth-exempt, after PR #149's status probe). Auth posture decision was made up-front via the new step 7 from sibling PR #53 — the polling-loop consumer profile contradicts a per-request internal-key requirement
  - **Modified** `backend/app/services/surfaces_catalog.py` (+9) — `batch_status` entry, integration type; catalog 31 → 32
  - **Modified** `backend/openapi.yaml` (+205) — path entry + `BatchStatusResponse` + `BatchStatusEntry` schemas
  - **New** `backend/tests/test_unit_batch_status.py` (+534) — 26 offline tests: cap, ordering, public-completed envelope, running null-analytics, failed null-analytics, **private-indistinguishable-from-unknown** (same `{found: false, ...nulls}` shape so anonymous callers can't probe for private existence), duplicate handling, id validation (path traversal, slashes, non-strings), empty/missing `sim_root`, mixed-case status, quality `"N/A"` fallback, catalog drift, auth-guard drift, route header drift, openapi drift, JSON-serialisability, corrupt `state.json` tolerance
  - **Modified** `docs/API.md` (+1), `docs/API.zh-CN.md` (+1), `docs/FEATURES.md` (+53) — note: only the second mention of `API.zh-CN.md` getting a row in a single PR, a tiny ongoing sign of the zh-CN doc surface staying in lockstep
  - **Modified** `frontend/src/api/simulation.js` (+45) — `batchSimulationStatus(simIds)` helper

- `7b79e7b` — `feat: signed-result.json — HMAC-SHA256 offline-verifiable signal payload (#152)` — merged 2026-06-08T12:40:12Z by Aaron
  - **New** `backend/app/services/signed_result.py` (+215 lines, pure stdlib `hmac`+`hashlib`+`json`+`datetime`) — `build_signed_result()` envelope builder, `canonical_json()` locks `sort_keys=True, separators=(",",":"), ensure_ascii=True` so the byte string a recipient reconstructs from a parsed `result` dict matches what was signed, `compute_signature()` primitive, `SCHEMA_VERSION = "1"` + `ALGORITHM = "hmac-sha256"` as published constants, `_KEY_HINT_LENGTH = 8` for the displayable secret prefix. **Missing-secret path returns 200 with `signed=false` + the unsigned result + a human-readable error** — never raises, never 500s, never withholds the underlying signal fields. The signing key is the *existing* `WEBHOOK_SECRET` — same algorithm + recipient-side primitive (`hmac.compare_digest` / `crypto.timingSafeEqual`) as the outbound webhook delivery service, so an integrator who already wrote a webhook verifier can verify stored signals with the same code
  - **Modified** `backend/app/api/simulation.py` (+112) — route handler `get_signed_result_json` on `simulation_bp`, inherits the `signal.json` publish gate (403 for private, 404 for not-yet-ready, byte-identical envelopes so the gate can't be probed), bumps a new `signed_result` counter inside `surface_stats`, `Cache-Control: max-age=300` matches `signal.json` cadence. Auth-posture comment in the docstring per the new step 7 from PR #53
  - **Modified** `backend/app/services/surface_stats.py` (+1) — `signed_result` added to `SURFACE_KEYS`
  - **Modified** `backend/app/services/surfaces_catalog.py` (+10) — 34th catalogued surface (integration type); `_PER_SIM_TRACKED_KEYS` extended in lock-step (drift test guards both)
  - **Modified** `backend/openapi.yaml` (+168) — path entry + `SignedSimulationResult` schema, with Python and Node.js verification recipes inline in the description
  - **New** `backend/tests/test_unit_signed_result.py` (+499) — 25 offline tests: envelope shape, `hmac.compare_digest` round-trip, `sort_keys`-stable canonical JSON, ASCII unicode-escape encoding, determinism across repeat calls, signature-tampering invalidation, sim-id binding, unsigned-path safety (None / empty / whitespace secret), `signing_key_hint` shape (first 8 hex chars of `sha256(secret)`), `surface_stats` registration, catalog entry, openapi drift, ISO-8601 `signed_at`, 64-char lower-hex signature, auth-posture comment present in handler
  - **Modified** `backend/tests/test_unit_surface_stats.py` (+1) — the second commit in the PR-squash: `signed_result` had to be added to the exact-equality expected set inside the existing serve-handler coverage guard, otherwise the drift test failed on CI. Caught by CI exactly as intended
  - **Modified** `docs/API.md` (+1) and `frontend/src/api/simulation.js` (+44) — Publish/Embed/Export row with verification recipe; `getSignedResultJson()` + `getSignedResultJsonUrl()` helpers

**Impact:** Three different consumer audiences just got first-class surfaces. (a) **Researchers + Aeon's own digest skills** can hit `/api/stats/distribution.json` once instead of computing buckets from a per-sim crawl. (b) **Batch-pollers** like AntFleet's benchmark pipeline and Capacitr's settlement loop collapse N HTTP polls into one — the surface explicitly treats private sims and unknown sims as byte-identical so it can't be used as a private-existence probe. (c) **Anyone storing a signed signal off-platform** (Capacitr's settlement ledger, research archives, ML provenance rows) can verify origin from any local copy without a live API call — the canonical-JSON + HMAC-SHA256 envelope pairs with the existing webhook delivery primitive, so the recipient-side code is already written for half the integrator base. The three PRs together push the catalog 31 → 34 entries, extend the 41-PR zero-deps streak to **42 PRs**, and close out three of the five Jun-06 repo-actions ideas (unbuilt: #2 Simulation Payload Validator, #4 Monthly Stats Time-Series, #5 Platform Agent Behavior Census).

---

## aaronjmars/miroshark-aeon

### Theme 2: Skill-prompt self-improvement — encode the near-miss
**Summary:** Two PRs, both edits to the prompt of a skill that had just hit a near-miss. PR #53 adds a step 7 to the `feature` skill making the auth posture an upfront decision (triggered by PR #149's three-commit auth-posture rewrite on Jun 5). PR #54 expands the `repo-pulse` skill to enrich every new stargazer + forker with a one-line GitHub profile summary, surfacing context (name, company, location, follower count, low-signal flag) that a bare handle hides. Same pattern as the recent `blocked-features` (PR #50) and `pre-existing-features` (PR #52) registries: each near-miss earns a new step in the skill that hit it.

**Commits:**

- `23410e4` — `improve: feature skill — decide auth posture upfront (#53)` — merged 2026-06-07T15:50:32Z by Aaron
  - **Modified** `skills/feature/SKILL.md` (+19/-5) — new explicit step 7 between pre-existence-grep (step 6) and implementation (step 8). Three required questions before any code is written: (a) is the consumer profile public-by-design or auth-required? (b) can an anonymous caller use the surface to *infer* private state (i.e. probe existence of private sims by comparing 403/404 vs 200 shapes)? (c) what does the openapi.yaml say about sibling routes? Decision must be wired in the **same commit** as the route handler — public goes in `internal_auth_guard`'s allow-list, private relies on the blueprint default, mixed handles per-method
  - **Modified** `memory/MEMORY.md` (+1) and `memory/logs/2026-06-06.md` (+13) — log entry recording the trigger, decision, and the encoded change
  - **New** `dashboard/outputs/self-improve-2026-06-06T13-42-59Z.json` (+228) — full json-render dashboard payload for the self-improve run that produced this PR
  - **Modified** `.outputs/self-improve.md` (+10/-9) and `memory/token-usage.csv` (+1) — cron output snapshot + token accounting

- `efb0c7c` — `repo-pulse: enrich new stargazers/forkers with profile info (#54)` — merged 2026-06-08T12:52:37Z by Aaron
  - **Modified** `skills/repo-pulse/SKILL.md` (+26/-8) — new step 5b: for each new stargazer + new fork-owner inside the 24h window, call `gh api users/$LOGIN --jq '{login, name, company, bio, location, blog, twitter_username, followers, public_repos, hireable, created_at}'` and surface a one-line summary in both the notification and the saved article. Hard cap of **25 enriched accounts per run** (stargazers + forkers combined) — beyond that, the first 25 are enriched in `starred_at` / `created_at` order and the rest become a single `…and N more` row. **Low-signal flag** appends ` ⚠ new/low-signal` when `followers ≤ 2 AND public_repos == 0 AND created_at within last 30d` — a soft fake-star tell that complements `star-milestone`'s burst check (annotate, don't suppress). Format is fixed: `${name or login} · @ ${company} · ${location} · ${followers}f · ${public_repos} repos · "${bio trimmed to ~80 chars}"`, dropping any segment whose source field is empty. `twitter_username` / `blog` go in the saved article only (not the notification, to keep messages short). Falls back to bare handle on lookup failure
  - The PR description's own example renders the value: a generic `github.com/alice` becomes `Alice Chen · @ Vercel · San Francisco · 2.3k followers · 87 repos · "building dev tools"` — operator gets context at a glance instead of having to click through

**Impact:** Two skill-level changes that target two different failure modes — *quality of decisions* and *quality of context*. PR #53 catches one of the most expensive `feature`-skill near-misses (PR #149 needed three review-commits to get the auth posture right) by making the decision explicit and front-loaded; the proof it works is already on the books — PR #150 in this same recap got the auth posture right on the first commit, and the PR's own merge log credits the new step 7. PR #54 fixes a quieter problem: most days `repo-pulse` notifies "3 new stargazers" with three bare handles and no operator-readable context. The enriched form makes the difference between "3 random logins" and "engineer at Vercel + early-career indie hacker + 6-day-old empty account ⚠" visible in the notification itself, with the low-signal flag specifically aimed at the fake-star problem MEMORY.md flags as a watched class of signal.

---

## Developer Notes
- **New dependencies:** none. Three MiroShark services added (`outcome_distribution.py`, `batch_status.py`, `signed_result.py`) — all pure stdlib (`os`/`json`/`time`/`threading`/`datetime`/`re`/`hmac`/`hashlib`). Zero-deps streak extended to **42 consecutive feature PRs**.
- **Breaking changes:** none. Every new endpoint is additive; existing surfaces unchanged.
- **Architecture shifts:** the auth-posture wiring point on MiroShark has now stabilised. PR #150's `/api/simulation/batch-status` is the second `/api/*` endpoint to opt out of the `internal_auth_guard` default at the allow-list (after PR #149's `/api/status.json` on Jun 5). PR #152's `/api/simulation/<id>/signed-result.json` opts the other way — into the per-sim publish gate inherited from `signal.json`. Both decisions were made up-front via the new step 7 from PR #53, not by mid-PR rewrite. The pattern is now: explicit at design time, never rewritten in review.
- **Tech debt:** none introduced. The `surface_stats.SURFACE_KEYS` exact-equality drift guard caught the `signed_result` omission on CI inside PR #152 (second commit in the squash) — the kind of guard that pays for itself.
- **Self-improvement loop in evidence:** PR #53 (feature-skill auth-posture step) and PR #54 (repo-pulse profile enrichment) are both the same shape as PR #50 (blocked-features registry) and PR #52 (pre-existing-features registry) — encode the near-miss as a step. Two new instances in this recap window.

## What's Next
- **PR #152 is freshly merged on MiroShark and the open-Aeon PR queue is back to 0** (was 0 yesterday Jun 7, the first 0-day in 17 days; today it returned to 0 after PR #152's merge). The next `repo-actions` run (Jun 10 per even-day parity, or earlier if Aeon picks up the slack) will likely pick from today's fresh batch: Simulation Activity Feed (`/api/activity.json`), Trending Simulation Topics (`/api/stats/topics.json`), MCP Tool Catalog JSON (`/api/mcp/tools.json`), Simulation Pre-Run Cost Estimator (`POST /api/simulation/estimate`), or Chinese README (the last of which targets the Jun-15 Chinese-locale hyperstition).
- **Three Jun-06 batch ideas are still unbuilt:** #2 Simulation Payload Validator, #4 Monthly Stats Time-Series, #5 Platform Agent Behavior Census. All blocked-feature exclusions reset Jun 13 per the 7-day window.
- **No new branches were left open on either repo.** The `feat/signed-result-json`, `feat/batch-status`, `feat/outcome-distribution`, `improve/feature-auth-posture-check`, and `repo-pulse-enrich-profiles` branches all merged and were deleted as part of their PR squash.
- **No issues opened or closed this window.** Community issue #95 (French locale) remains open and non-urgent — PR #148's i18n helper test coverage from earlier this week remains the standing prerequisite for the dict-form `_t()` refactor whoever picks it up will need to ship.
- **Repo-pulse will produce noticeably richer output starting tomorrow** — the next stargazer line will read like `Alice Chen · @ Vercel · 2.3k followers` instead of `github.com/alice`. The fake-star ⚠ flag is the first time `repo-pulse` will annotate suspicious accounts inline, and it'll be informative regardless of whether any actually fire (zero ⚠ flags in a day is also signal).
