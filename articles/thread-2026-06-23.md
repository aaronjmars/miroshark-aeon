# Thread — 2026-06-23

**Topic:** cost CLI subcommand — PR #208 on aaronjmars/MiroShark
**Score:** 6 (signals: new-feature-pr-on-watched-repo)
**Source events:**
- Feature — aaronjmars/MiroShark: `cost` CLI subcommand (cmd_cost at cli.py:201, `~$` prefix gated on is_estimate at cli.py:220, exit code 2 when no cost logged); PR #208 (cef787b, 06-23); 4 files: cli.py +81/-1, test_unit_cli.py, CLI.md, CLI.zh-CN.md; FEATURE_OK
- repo-article-2026-06-23: cost-auditability triad complete — cost.json API (PR #179, 06-16) → embed pill (PR #190, 06-19) → CLI (PR #208, 06-23); all three read the same cost.json; caveat ships in every surface

---

## Tweet 1
$1 to simulate anything. PR #208 makes that checkable from a terminal. `python cli.py cost <sim_id>` — the sim's dollar cost, locally, no dashboard login.

## Tweet 2
three cost surfaces now in MiroShark. cost.json API landed June 16 (PR #179) — lower bound on what a sim spent, flagged as an estimate since not all models are tracked. PR #190 put that number in the embed pill on the public sim page.

## Tweet 3
PR #208: `cmd_cost` in cli.py:201. the `~$` prefix mirrors the embed pill — both gate on `is_estimate`. exit code 2 when no cost recorded, so scripts can distinguish 'zero spend' from 'never ran'. 81 lines, 4 files.

## Tweet 4
all three surfaces read the same cost.json — one source, one undercount. that's the constraint: untracked models log as $0, so the number is a floor, not the full bill. shipping that caveat explicitly, in every surface, is the honest version of the claim.

## Tweet 5
PR #208 — cost CLI subcommand on MiroShark. https://github.com/aaronjmars/MiroShark/pull/208 🦈

---

**Character counts:** 1: 154 | 2: 238 | 3: 215 | 4: 255 | 5: 89
**Artifact link:** https://github.com/aaronjmars/MiroShark/pull/208
