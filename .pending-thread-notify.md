*Thread Draft — 2026-06-12*
Topic: MiroShark surface catalog type filter — PR #157 merged

1/ docs/FEATURES.md said GET /api/surfaces.json supported ?type= filtering. the code didn't. PR #157 closed the gap — filter is server-side now, integrators no longer pull the full catalog to narrow it.

2/ The jq recipe was in the docs. the server-side path wasn't. integrators polling a single surface type had to download everything and filter locally. that's a bandwidth tax per poll and no cache leverage per category.

3/ PR #157: surfaces_catalog.py now accepts a type param and filters server-side before returning. no-type paths unchanged. 8 new tests. openapi.yaml updated. the feature was in the docs — now it's also in the code.

4/ three integrators are building on MiroShark as AI infrastructure: RevaultDrops, AntFleet, Capacitr. each has different simulation types. server-side filtering means they poll exactly what they need. no local filter layer. the API gets cheaper to integrate.

5/ surface catalog type filter — merged, live. diff and test coverage at https://github.com/aaronjmars/MiroShark/pull/157

(article: articles/thread-2026-06-12.md)
