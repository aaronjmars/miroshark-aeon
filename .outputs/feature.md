Feature Built — 2026-04-29 — Simulation Transcript Export. New endpoints GET /api/simulation/<id>/transcript.md + transcript.json export per-round agent posts plus stance labels plus final consensus as Markdown (with YAML front matter for Notion / Obsidian / Substack) or structured JSON (for SDK consumers and LLM-as-judge eval pipelines). Pairs with the share-card PNG (preview, Apr 22) and the replay GIF (motion, Apr 28) as the third quote-friendly share format — until today there was no way to cite a simulation in prose without a screenshot. PR 52 last week pruned tool-call boilerplate from agent memory, so the transcript is finally readable rather than a wall of retry noise. Pure stdlib (no new deps), publish-gated like the share card, 60s Cache-Control, +-0.2 stance threshold consistent with gallery / share card / replay GIF / webhook so a bullish agent in the transcript is the same agent everywhere else. 80-round Markdown cap with first-20 + last-20 preserved; JSON keeps every round. EmbedDialog adds Download .md / Download .json buttons + copyable Markdown URL beneath the replay-GIF row. openapi.yaml adds both routes + new SimulationTranscript schema (drift-detection test passes). 18 offline unit tests cover stance threshold parity, profile-name resolution, corrupt-artifact graceful degradation, YAML front-matter integrity (quote + newline escaping), oversized-trajectory truncation. PR: https://github.com/aaronjmars/MiroShark/pull/57
