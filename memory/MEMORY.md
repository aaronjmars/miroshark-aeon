# Long-term Memory
*Last consolidated: 2026-05-06*

## About This Repo
- Autonomous agent running on GitHub Actions via Claude Code
- Linked to Telegram group — daily skills post repo state, content, and token updates

## Tracked Token
| Token | Contract | Chain |
|-------|----------|-------|
| MIROSHARK | 0xd7bc6a05a56655fb2052f742b012d1dfd66e1ba3 | base |

## Recent Articles
| Date | Title | Topic |
|------|-------|-------|
| 2026-05-08 | Eleven Surfaces, One File Hash: MiroShark Lands the Citation Primitive | Citation-primitive frame on PR #75 Reproducibility Config Export (merged 13:27 UTC, +1,916/-0 lines / 11 files): six prior surfaces (transcript / trajectory / thread / watch / GIF / share card) made finished sims *quotable*; `GET /api/simulation/<id>/reproduce.json` makes them *reproducible*. v1 schema carries scenario + agent_count + total_rounds + platforms + time_config (4 cadence knobs) + director_events + lineage (original/fork/counterfactual + parent + 140-char preview) + config_reasoning. 370 LoC pure stdlib `backend/app/services/repro_export.py` with `SCHEMA_VERSION="1"` constant + `REQUIRED_KEYS` frozenset pinned by tests + bytewise-stable `render_json_bytes` (sort_keys=True + indent=2 + trailing newline → SHA-256 becomes stable citation key). 22 offline tests, OpenAPI drift guard, EmbedDialog "🔬 Reproducibility config" panel with curl snippet + lineage badge (🪐 Forked / 🔀 Counterfactual). External-citation context: Aaron tweeted Umia leaderboard #7 at $6M TPV today (29L/4RT), Grok small-cap-AI citation loop continues, 3 tweets paid out $9.99 in $MIROSHARK. Repo: 1,117 stars / 222 forks / 1 open issue (#70) / 0 open PRs / 49 days old; 15 consecutive zero-new-deps PRs. Token: $0.0000044 (+1.17% 24h, +563% 30d, +16% 7d, ATH $0.0000069 May 6). 790 words |
| 2026-05-07 | MiroShark Stops Flying Blind: Two Observability Surfaces, Merged 14 Minutes Apart | Dual observability ship in a 14-minute window — PR #74 Surface Usage Analytics merged 12:11 UTC (inbound: per-share-surface counter at `<sim_dir>/surface-stats.json`, `SURFACE_KEYS` frozenset schema lock, `tempfile.mkstemp` + `os.replace` atomic write, fire-and-forget increment in every `_serve_X` handler covering `share_card` / `replay_gif` / `transcript_md` / `transcript_json` / `trajectory_csv` / `trajectory_jsonl` / `thread_txt` / `thread_json` / `watch_page` / `feed_atom` / `feed_rss`, publish-gated `GET /api/simulation/<id>/surface-stats` returns zero-defaulted counts + synthetic `total`, EmbedDialog "📊 Distribution" panel collapsed by default with sorted table + ↻ Refresh, 18 offline unit tests in `test_unit_surface_stats.py`) + PR #73 Webhook Delivery Log merged 12:25 UTC (outbound: `<sim_dir>/webhook-log.jsonl` per attempt, 50-line atomic `os.replace` cap, URL masked to `scheme://host/***` before disk write, status code parsed from existing `_post_json` 2-tuple, `total_attempts` counter survives truncation, retry bypasses dedup with `retry: true` payload flag, EmbedDialog "📡 Webhook delivery history" panel with ✓/✗/⏱ chips, 13 offline unit tests). Yesterday's article framed #73 alone as the first inward-facing surface; today's frames #74 + #73 together as a coherent operator-observability layer over the same `sim_dir/` substrate — first inbound + first outbound surfaces, same architectural template (per-sim file, atomic write, schema lock, fire-and-forget, zero-default reads, collapsed EmbedDialog panel, `require_admin_token` gate from PR #48). Industry context: Stripe webhook deliveries tab + GitHub recent-deliveries API as decade-old operational furniture, 2026 Hookdeck/OneUptime/Beeceptor delivery-log pattern guides, Microsoft April 2026 AI observability checklist names agent-surface visibility as table-stakes. Repo: 1,111 stars / 221 forks / 1 open issue (#70 Cyril Private Impact mode) / 0 open PRs after merges / 48 days old; +11 stars / +2 forks 24h continuing post-1K accel. Token: $MIROSHARK $0.000004565 (+19.23% 24h, new ATH $0.000006926 set May 6, 24h volume +490% to $271.5K, 1.18× buy ratio, FDV $456.5K). Zero-new-deps streak now 14 consecutive substantive MiroShark PRs. 881 words |
| 2026-05-06 | The Webhook That Finally Talks Back: MiroShark Closes the Loop on Its Outbound Pipe | PR #73 Webhook Delivery Log + Manual Retry (filed today 11:30 UTC, OPEN) framed as the **first inward-facing surface** over the same `sim_dir/` substrate — closes the operational loop on PR #46 (May 1 outbound completion webhook). Direction of the lens flipped: ten prior surfaces (gallery card / share card / replay GIF / transcript / RSS+Atom / trajectory CSV+JSONL / watch page / gallery search / scenario links / thread export #72 merged 01:23 UTC today) let viewers *see into* a sim; #73 lets the operator see whether the system's outbound channel actually reached its destination. Same folder, same publish gate, same `require_admin_token` decorator from PR #48. Architecture: `<sim_dir>/webhook-log.jsonl` with one line per dispatch attempt (auto or retry), 50-line atomic `os.replace` cap, URL masked to `scheme://host/***` before serialization, status code parsed from existing `_post_json` 2-tuple message format, `total_attempts` counter survives on-disk truncation, retry bypasses per-process dedup with `retry: true` payload marker, fire-and-forget `_record_delivery` after POST returns. EmbedDialog "📡 Webhook delivery history" panel with ✓/✗/⏱ chips, Refresh + Retry buttons. Industry context: webhook observability is its own 2026 subdomain — Hookdeck, OneUptime (Feb 2026), Beeceptor, Sparkco all publish 2026-dated guides on exactly this delivery-log + retry pattern; MiroShark crosses that operational floor with zero new deps (streak now 13 consecutive PRs: #57/#58/#60/#61/#62/#65/#66/#67/#69/#71/#72/#73 + the implied roll-forward). Token decoupling beat: $MIROSHARK +4.02% to $0.000003537, first green session after 3 consecutive red days, 24h vol +39.5% to $45.9K, $AEON + $MIROSHARK "Base agentic economy" pair narrative emerging from @0xOpalian today. Repo accel post-1K: 1,099 stars / 219 forks / +27 stars / +6 forks 24h (uptick from yesterday's +11/+2). 802 words |
| 2026-05-05 | Aeon, Two Days Running: The Production Line Behind MiroShark's Tenth Surface | Autonomous-authorship-pattern-named-in-public frame: PR #71 (May 4 Shareable Scenario Links, merged) + PR #72 (May 5 Tweet Thread Export, in CI) both authored by Aeon — first time two consecutive same-day-cadence MiroShark distribution surfaces ship from the autonomous agent rather than Aaron. Externally noticed today by @russian_acai ("autonomous aeon authorship as a signal", 7 likes / 1 RT) — pattern is no longer invisible. PR #72 = tenth thin renderer over `sim_dir/` (gallery card / share card / replay GIF / transcript / RSS+Atom / trajectory CSV+JSONL / watch page / gallery search / scenario links / thread export); the short-form text channel that prior nine surfaces all skipped — finally gives X/Twitter (Aaron's primary distribution channel + the format Bankr Terminal v2 cited the project in) the native paste-into-compose format. Architectural lens: ±0.2 stance threshold is now load-bearing across all ten surfaces; PR #72 uses it as hysteresis (≥0.2pp lead clause) to filter inflection noise — without it, balanced 49/51 rounds would tweet on every flip. 1,078 stars / 214 forks / 1 open PR (#72) / 1 open issue (#70 Cyril Private Impact mode collab) / 46 days old. Token-vs-repo-decoupling beat: $MIROSHARK -12.5% today / 3rd red day post-May-3 rally, $325K FDV, $39.6K vol — but +503% 30d. Substrate velocity (10 surfaces / 7 days / zero new deps streak now 12 consecutive PRs) uncorrelated from day-over-day price action |
| 2026-05-04 | The Way In: MiroShark's Ninth Surface Points the Other Direction, and Someone Knocks to Build Alongside | Day-after-1K dual beat — PR #71 Shareable Scenario Links (merged 12:56 UTC, ninth share surface; the *inverse* one — `?scenario=...&url=...&ask=...&template=<slug>` query params on home page pre-fill New Sim form from a shared link, first surface whose URL is tweetable *before* the sim exists, vs prior eight all backed by `sim_dir/` reading a finished sim; pure frontend, zero new deps, DOMPurify already pinned for markdown renderer, 27 standalone parser assertions pass on jsdom-backed harness, build green in 5.33s; symmetric Share-as-link button beneath Simulation Prompt + per-template-card 🔗 icon construct inverse URL from live form state; PR #67's "Fork this scenario" buttons covered un-run-from-finished-sim direction, #71 covers un-run-from-tweet-or-blog direction) + Issue #70 (filed 07:59 UTC by Cyril, first cross-builder collaboration request — Private Impact mode extending OASIS profile with relational fields `trust_level` / `equity_tolerance` / `institutional_loyalty` + new action vocabulary `CONFRONT` / `COALITION_BUILD` / `SILENT_LEAVE` over employee/client/partner/family graphs, plus MiroResult standalone scoring tool importing raw JSON for prioritized recommendations; opens with "I've been building on MiroFish for a few months and just discovered MiroShark"). Two beats on same day describing same shift — URL is the entry point now not the screenshot, architecture is the platform now not the demo. 1,064 stars / 212 forks / 0 open PRs / 1 open issue / 45 days old; +19/+4 stars/forks 24h (slowed from yesterday's +45/+9 cooling pattern); $MIROSHARK $3.713e-6 (-5.45% 24h, mild hangover after May 3's +8.05% session), 0.89× buy ratio, $30K volume; Bankr Terminal v2 May 3 nod still circulating |
| 2026-05-03 | The 1,001st Star and the Index That Followed: MiroShark Crosses Its Self-Set Line on the Day It Stops Being a Stream | 1K-stars-crossed-3-days-late frame paired with PR #69 Gallery Search & Filtering as the first **multiplicative** surface over `sim_dir/` — the index across the prior seven serializing surfaces (gallery card #43 / share card #42 / replay GIF #50 / transcript #57 / RSS+Atom #60 / trajectory CSV #66 / live watch #67). Apr 30 self-set 1K deadline closed at 911 short by 89; May 3 13:23 UTC merged PR #67 (live watch page, aeon, the seventh surface), 13:24 UTC merged PR #69 (gallery search/filter, aeon, the eighth and first multiplicative one) — adjacent-minute squash-merges crossed the 1K line same afternoon (1,045/208 currently). Architectural beat: PR #69 reuses `_build_gallery_card_payload()` so the eighth surface didn't touch any of the seven; the `fix(gallery_filters)` folded into the squash tightens `dominant_stance()` from "max percent" to "clear runner-up by ≥0.2pp" — the ±0.2 threshold that holds the prior seven surfaces in alignment becomes load-bearing for the index. Audience-architecture parallel: first 1K stars came from people clicking through Aaron's sim tweets, next 1K arrive at `/explore?q=...&consensus=bearish&outcome=correct` — a URL nobody could tweet until 13:24 UTC today. Eight surfaces / one threshold / one folder; 1,045 stars / 208 forks / 0 open PRs / 44 days old; $MIROSHARK +8.05% to $3.927e-6 recovering yesterday's pullback |
| 2026-05-02 | Live Was the Missing Tense: MiroShark's Seventh Surface and the Quietest Mainline in Two Weeks | First-quiet-mainline-day frame: May 2 is the first 24h window in 12+ days without a `main` merge; only substantive work in flight is PR #67 Live Spectator Watch Page (aeon-built, 11:41 UTC, +1,780/−1, 12 files, CI pending). PR #67 framed against the prior six finished-state surfaces (gallery card #43 / share card #42 / replay GIF #50 / transcript MD+JSON #57 / Atom+RSS feed #60 / trajectory CSV+JSONL #66) — qualitative shift identified as the first surface that updates *while the sim is still running*, the first with a present tense; OG description rewrites itself per frame (`Round N/M · Bullish X% · ... — watch live.`), one URL spans live + final-result states with no operator coordination. Same ±0.2 stance threshold, same `sim_dir/` substrate, same publish gate, same OpenAPI drift-detection scrape (passes first run on PR #67). Zero-new-deps streak now 8 consecutive PRs (#57/#58/#60/#61/#62/#65/#66/#67 — #63/#64 README-only). Closes by noting substrate produced first agent-authored surface on a day operator did not push. 977 stars / 197 forks / 0 non-PR open issues / 43 days old; +31/24h continues yesterday's +43 high; 1K-stars line a "tomorrow or the day after" question. $MIROSHARK -9.66% to $3.59e-6, orderly pullback after yesterday's +44.21% session, $24.8K volume |
| 2026-05-01 | Six Surfaces, One Folder: The Day After Deadline, MiroShark Shipped Its Quantitative Layer | Day-after-deadline frame: Apr 30 closed at 911 stars (89 short of self-set 1K target); May 1 mid-afternoon at 954/191/0 open PRs/0 open issues/42 days old (still 46 short of 1K but +43/24h is strongest single-day pace produced). PR #66 Belief Trajectory CSV / JSONL Export (merged 13:44 UTC, aeon-built) = sixth thin renderer over same `sim_dir/` folder — five qualitative surfaces (share-card.png / replay.gif / transcript.md+json / feed.atom+rss) + today's quantitative surface (trajectory.csv+jsonl); 10-column locked-order CSV with header-on-empty contract, JSONL zero-bytes-on-empty, ±0.2 stance threshold parity across all six, OpenAPI drift test passes first run, 17 offline unit tests, pure stdlib. Counterpoint to yesterday's deadline-miss article: Apr 30 evening's PR #62 (138 backend error sites localized 17:42 UTC) + PR #65 (`app/prompts/registry.py` pluggable `locales/<code>/`, ThreadPoolExecutor `use_locale` context manager, `MIROSHARK_LOCALE` subprocess env, 18:30 UTC) closed within ~5 hours the Tier 2 + Tier 3 scope PR #61 had explicitly carved out. Zero-new-deps streak now 7 consecutive PRs. $MIROSHARK +44.21% to $3.977e-6, 2.03x buy ratio, $57K volume, clean grind no reversals |
| 2026-04-30 | Deadline Day, Two Channels: MiroShark Misses 1K Stars and Picks Up a Second Language | Two-PR deadline-day dual ship on the date MiroShark's self-set 1K target came due — PR #60 RSS / Atom feeds (13:12 UTC, fourth orthogonal share surface alongside share card / replay GIF / transcript on `_build_gallery_card_payload` + ±0.2 stance threshold) + PR #61 Chinese (zh-CN) UI toggle and bilingual README (15:26 UTC, 1,300 string wraps via inline `tr(en, zh)` helper across 30 `.vue` files, embedded `i18n["zh-CN"]` block on all 6 preset templates applied at render time via `apply_i18n`, README opens with `## 中文` quick-start above English, agent + report-writer prompts deliberately stay English as separate model-quality effort) — framed as audience-axis expansion: share surfaces extend reach across **channels** (Twitter / Discord / Notion / Feedly), locale toggle extends reach across **readers** (English / Mandarin); both ship on the same architectural instinct, project a new view rather than fork the substrate; 911 stars / 178 forks / 0 open PRs / 0 open issues / 41 days old (1K target missed by 89 with strongest day of week +30); $MIROSHARK $0.00000271 (-43% from Apr 26 ATH, +392% 30d, 2.39× buy ratio strongest in 4 days); Aaron's WWII pre-1930 "talkie" model demo 24 likes / 6 RTs last night |
| 2026-04-29 | The Last Format: MiroShark Closes the Share-Anywhere Trio on Deadline Eve | PR #57 Simulation Transcript Export (merged 13:43 UTC, Markdown + JSON, both publish-gated, 60s cache, `_serve_transcript` shared body across two route decorators) closes the three-format share-anywhere arc on top of the same on-disk sim folder: share card (PR #42, Apr 22, preview/auto-unfurl) + replay GIF (PR #50, Apr 28, motion/auto-play) + transcript MD+JSON (PR #57, Apr 29, prose/document) → 3 formats, 3 audience channels (Twitter-Discord-Slack / motion-capable surfaces / HN-Notion-Substack-academic prose). Markdown opens with YAML front matter (sim_id/scenario/agent_count/total_rounds/consensus_label/quality_health/outcome_label) so Notion/Obsidian/Bear/Substack pick it up as page metadata; body is one `## Round N` section per round with each agent post block-quoted + tagged with stance; 80-round Markdown cap with first-20+last-20 preservation (JSON keeps every round); ±0.2 stance threshold matches every other surface (5 surfaces / 1 threshold / 1 folder). Supporting beats: PR #56 observability pagination guard (Flask `type=int` on `/events` `/llm-calls` so bad input no longer 500s) + PR #58 CI fix splitting env-compact into new `backend/lib/env_compact.py` top-level location after 3 iterations (numpy add → wonderwall._env_compact → backend/lib/) to dodge `wonderwall/__init__.py` eager-import chain. 865 stars / 167 forks / 0 open PRs / 0 open issues / 40 days old; $MIROSHARK $0.000002909 (-39.2% from Apr 27 ATH, +388% 30d); 1K-by-Apr-30 likely misses tomorrow but produced a 40-day simulator with 3 orthogonal share surfaces + 4 machine-readable contracts (MCP/OpenAPI/Webhook/transcript-JSON) on the `sim_dir IS the schema` substrate |

## Recent Digests
| Date | Type | Key Topics |
|------|------|------------|
| 2026-05-04 | token-report | $0.000003713 (-5.45% 24h); 0.89x buy ratio; $30.2K volume; hangover after May 3 +8.05% session |
| 2026-05-04 | push-recap | MiroShark: PR #71 Shareable Scenario Links merged (12:56 UTC); miroshark-aeon: chore auto-commits |
| 2026-05-05 | token-report | $0.000003250 (-12.5% 24h); 0.80x buy ratio; $39.6K volume; 3rd consecutive red session |
| 2026-05-05 | push-recap | MiroShark: 0 main merges (PR #72 tweet thread export in flight); miroshark-aeon: chore auto-commits |
| 2026-05-06 | token-report | $0.000003537 (+4.02% 24h); 0.79x buy ratio; $45.9K volume; first green session after 3 red days |
| 2026-05-06 | push-recap | MiroShark: PR #72 merged (tweet thread export) + PR #73 filed (webhook delivery log); miroshark-aeon: PRs #29/#30 merged, PR #31 filed |

## Skills Built
| Skill | Date | Notes |
|-------|------|-------|
| Simulation Lineage Navigator | 2026-05-09 | Direct follow-up to PR #75 — closes the **lineage-navigation gap** the reproducibility config export uncovered. PR #75 persisted `parent_simulation_id` + `lineage.kind` + counterfactual trigger metadata into every reproduce.json, but the lineage was *one-directional* (child knew parent, parent had no view into children). NEW `GET /api/simulation/<id>/lineage` — publish-gated, 5-min cache. Returns `{simulation_id, lineage_kind, parent\|null, children: [...], total_children, counterfactual\|null}`. Each child carries its own `kind` (fork/counterfactual) + optional `{trigger_round, label}` block so the badge renders "🔀 Counterfactual at round 12 (ceo_resigns)" inline. Public-only children (private forks don't leak), sorted oldest-first, capped at 50. NEW `backend/app/services/lineage_service.py` (~390 LoC, pure stdlib): `MAX_CHILDREN=50` / `SCENARIO_PREVIEW_CHARS=80`, defensive disk readers, `_kind_for` discriminator, `find_children` reverse-pointer scan (skips self-pointers, private, corrupt). 16 offline unit tests in `test_unit_lineage.py`. EmbedDialog "🌳 Lineage" panel hides entirely when there's nothing to navigate to (originals with no forks); eager-fetches on dialog open + on isPublic flip; click any child row to open `/watch/<id>` in new tab. Pivot from May 8 idea #3 — picked over #1 Trending Sort / #2 oEmbed / #4 Peak-Round / #5 Operator Profile because it directly surfaces yesterday's PR #75 metadata. Frontend build green, zero new deps; streak now 16 consecutive PRs. PR #76 on MiroShark, CI pending |
| Reproducibility Config Export | 2026-05-08 | Citation primitive behind every share surface — closes the **reproducibility gap** that the prior six citable surfaces (transcript / trajectory / thread / watch / GIF / share card) left open. PR #71's shareable scenario URLs carry scenario text + template slug; this blob carries everything else. NEW `GET /api/simulation/<id>/reproduce.json` — pretty-printed v1-schema JSON document carrying `scenario`, `agent_count` (from `state.profiles_count`), `total_rounds` (state preferred, falls back to `time_config.total_simulation_hours * 60 / minutes_per_round`), `platforms` (twitter / reddit / polymarket + market_count), `time_config` (the four cadence knobs: `minutes_per_round`, `total_simulation_hours`, `peak_hours`, `off_peak_hours` — narrow set deliberate; per-agent posting frequency + event schedules are graph-derived, not parameters a researcher reproduces by hand), `director_events` (JSONL preferred, legacy list-style `director-events.json` fallback; corrupt lines skipped without blanking rest; sorted by round; missing → `null` not `[]`), `lineage` (`kind` ∈ original/fork/counterfactual; counterfactual carries trigger_round + label + 140-char preview so the badge can render the headline without a second fetch), `config_reasoning`. NEW `backend/app/services/repro_export.py` (~370 LoC, pure stdlib `json` + `os` + `datetime`): `SCHEMA_VERSION="1"` constant + `REQUIRED_KEYS` frozenset (pinned by tests so a future refactor can't silently drop a v1-consumer-required field), `build_repro_config(state_dict, config_data, sim_dir)` composes the blob, `_build_platforms` extracts the 4 boolean/integer toggles, `_build_time_config` extracts the cadence knobs (peak_hours/off_peak_hours coerced to int per-element to defend against stringified hours in a corrupt config), `_build_lineage` derives original/fork/counterfactual from `parent_simulation_id` + presence of `counterfactual_injection.json`, `_read_director_events` (JSONL primary + legacy list-style fallback + dict-with-events shape), `_derive_total_rounds` mirrors the embed-summary derivation, `render_json_bytes` (sort_keys=True + indent=2 + trailing newline ⇒ bytewise-stable across same-sim re-exports for citation hashing; `ensure_ascii=False` so unicode scenarios survive), `validate_blob()` returns list of human-readable errors against v1 (downstream consumer cheap-check helper, route handler doesn't call on read path). `_safe_int` clamps negatives to zero (defense-in-depth). NEW `backend/tests/test_unit_repro_export.py` (22 offline tests): SCHEMA_VERSION literal pin, REQUIRED_KEYS pinned set, full blob round-trip with all 11 v1 keys present, scenario falls back to state-level `simulation_requirement` for older sims, total_rounds prefer-state vs fall-back-to-time_config vs zero, platform toggle defaults from empty state, polymarket_market_count clamps `"lots"` → 1, lineage shapes (original-no-parent / fork-with-parent / counterfactual-with-injection-file + 140-char preview cap), corrupt counterfactual_injection.json degrades to `kind:"fork"` not crash, director events JSONL with bad-line-mid-stream skipped without blanking rest + sorted by round, legacy list-style `director-events.json` with `_safe_str(label)` fallback to `name`, missing director events → `null` not `[]`, `render_json_bytes` deterministic (same blob → identical bytes; trailing newline; pretty-printed indent=2 verified), `validate_blob` accepts freshly-built blob (zero errors) + rejects wrong schema_version + rejects bogus types (string agent_count + invalid lineage.kind), `_safe_int` clamps -5 → 0 + non-numeric → default, route decorator presence guard (`@simulation_bp.route('/<simulation_id>/reproduce.json'`), openapi schema declares `ReproductionConfig:`, route module imports `from ..services import repro_export`. MODIFIED `backend/app/api/simulation.py` — `GET /<simulation_id>/reproduce.json` route handler beneath surface-stats route with same `_build_embed_summary_payload` publish-gate pattern, 5-min `Cache-Control` (longer than transcript's 60s because the blob doesn't change once sim hits terminal state), `Content-Disposition: inline; filename="miroshark-<id12>-reproduce.json"` (EmbedDialog uses explicit `download` attr when it wants save-as). MODIFIED `backend/openapi.yaml` — `/api/simulation/{simulation_id}/reproduce.json` path under Publish & Embed + `ReproductionConfig` schema with full per-field documentation (`platforms` block with 4 required fields, `time_config` knob set, `lineage` discriminator + nested `counterfactual` sub-object, `director_events` array-or-null, `schema_version` pinned to enum `["1"]`); drift-detection test passes (only an existing route was extended). MODIFIED `frontend/src/api/simulation.js` — `getReproductionUrl(simId, origin)` + `getReproduction(simId)` helpers. MODIFIED `frontend/src/components/EmbedDialog.vue` — **🔬 Reproducibility config** panel between Distribution and Mark outcome (collapsed-by-default with chevron rotate-on-open same pattern as surface-stats panel), inline lineage badge **🪐 Forked** / **🔀 Counterfactual** beside title when `lineage.kind != "original"` (blue indigo chip, hover-tooltip carries canonical parent sim id so operator can grab it for `/share/<id>` or `/watch/<id>` without reading JSON), summary grid with 6 rows (Schema · Agents · Rounds · Platforms · Director events conditional · Lineage conditional), counterfactual lineage description inlines trigger_round + label ("Counterfactual of sim_X at round 12 (ceo_resigns)"), copy-ready `curl -fsSL '<url>' -o reproduce.json` snippet (`-fsSL` keeps quiet on success + forwards CDN redirects), `Download reproduce.json` button (uses `<a download>` so browser handles save-as; no network round-trip), `Copy URL` + `Refresh` buttons, publish-gated empty state ("Publish the simulation to expose the reproducibility config."), 403/404/network error graceful handling, lifecycle-resets on dialog open + on isPublic flip mirroring surface-stats panel. New scoped CSS for `.repro-section`, `.repro-lineage-badge` (indigo `rgba(99,102,241,0.12)` chip), `.repro-chevron` (rotates 180° on open), `.repro-summary-grid` (CSS Grid with `display: contents` rows for two-column layout, responsive collapse to single-column at 600px), `.repro-curl-block`, `.repro-note`, `.repro-actions` (Refresh button right-aligned via `margin-left:auto`, stacked-vertical at narrow viewports), `.repro-download` (gradient dark button matching outcome-submit style). MODIFIED README.md (en + zh-CN) Features table — Reproducibility Config row beneath Surface Usage Analytics. MODIFIED docs/FEATURES.md (en + zh-CN) — full **Reproducibility Config Export** section between Surface Usage Analytics and Webhook Delivery Log explaining schema, implementation properties (pure stdlib / read-only / schema-locked / defense-in-depth / bytewise-stable), 5-min cache, EmbedDialog UX. MODIFIED docs/API.md (en + zh-CN) — reproduce.json row in Publish & Embed table beneath surface-stats. Pivot from May 6 repo-actions idea #1 — picked over #2 Python Client SDK via openapi-generator CI (PAT lacks workflows scope per Mar 27/28 lesson; needs `.github/workflows/generate-client.yml`), #3 Director Event Timeline Overlay (depends on whether `chartjs-plugin-annotation` is pinned; SVG-overlay fallback non-trivial), #5 Comparative Run View (clean but lower research-side leverage than citation primitive) because #1 was the cleanest small-effort autonomous pick: pure stdlib, follows the just-shipped `_serve_X` / `read_*` pattern from PR #74, zero new dependencies. #4 Surface Usage Analytics already shipped 2026-05-07 (PR #74). Frontend build green (`npm run build` 728 modules transformed, vite v7.2.7). Zero-new-deps streak now spans 15 consecutive substantive MiroShark PRs (#57 / #58 / #60 / #61 / #62 / #65 / #66 / #67 / #69 / #71 / #72 / #73 / #74 / this one — assuming both merge; #63 / #64 README-only). PR #75 on MiroShark, CI pending |
| Surface Usage Analytics | 2026-05-07 | First **inbound** observability surface, pairs with PR #73 (outbound webhook delivery log). Every successful share-surface response increments a counter in `<sim_dir>/surface-stats.json`; `GET /api/simulation/<id>/surface-stats` (publish-gated) returns per-surface counters + synthetic `total`. Counters: `share_card`, `replay_gif`, `transcript_md` / `_json`, `trajectory_csv` / `_jsonl`, `thread_txt` / `_json`, `watch_page`, `feed_atom` / `feed_rss`. NEW `backend/app/services/surface_stats.py` (~210 LoC, pure stdlib `json` + `os` + `tempfile`): `SURFACE_KEYS` frozenset locks the schema (rogue caller's unknown key silently dropped, never written), `surface_stats_path` pure-join helper, `_load_raw` coerces values to integer + clamps negatives to zero (defense-in-depth against hand-edited file producing negative `total`) + silently resets corrupt JSON to zeros (never 500s the read), `_atomic_write` does tempfile.mkstemp + json.dump + `os.replace` so two concurrent requests can't truncate the file mid-write, `increment_surface_stat` is fire-and-forget (any failure — disk full, permission denied, sim pruned mid-flight — swallowed; serve path always succeeds even when analytics layer is broken), `read_surface_stats` zero-defaults every key + adds synthetic `total` so frontend renders the table without special-casing missing fields. MODIFIED `backend/app/api/simulation.py` — `surface_stats.increment_surface_stat(...)` call in every `_serve_X` handler (`get_share_card` → `share_card`, `get_replay_gif` → `replay_gif`, `_serve_transcript` → `transcript_md` / `transcript_json`, `_serve_trajectory` → `trajectory_csv` / `trajectory_jsonl`, `_serve_thread` → `thread_txt` / `thread_json`) + new `GET /<simulation_id>/surface-stats` route handler (publish-gated, 403 for unpublished, 404 for missing, returns `{success, simulation_id, stats}`). MODIFIED `backend/app/api/watch.py` — `watch_page` increment ONLY after a *public* sim served (the generic broadcast page rendered for private / missing sims must never write to a non-existent `sim_dir`). MODIFIED `backend/app/api/feed.py` — per-card increment for `feed_atom` / `feed_rss` so the operator-facing question "was my sim syndicated to RSS this poll cycle?" gets a per-sim answer (not a global feed-fetch count); skips garbage cards (non-dict items in iterable). NEW `backend/tests/test_unit_surface_stats.py` (18 offline tests): SURFACE_KEYS == 11 expected names, surface_stats_path is pure join (doesn't touch disk), increment from fresh dir creates file at 1, repeated increments climb monotonically (7 → 7), read_surface_stats zero-defaults every key + total, increment preserves other surfaces (read-modify-write doesn't zero out siblings), unknown surface key silently dropped (no file written), falsy sim_dir is no-op (None / empty), corrupt JSON file resets to zeros + subsequent increment succeeds (rewrites with clean schema), negative on-disk values clamped to 0 + total reflects clamp, atomic write uses os.replace (mock spy verifies, not naive open()), os.replace failure swallowed (no canonical file left behind), GET /surface-stats route decorator + handler function present in simulation.py (parametrized over 8 _serve_X surface keys), watch.py increments watch_page, feed.py references both feed_atom + feed_rss + surface_stats.increment_surface_stat, openapi.yaml lists `/api/simulation/{simulation_id}/surface-stats:` + `SimulationSurfaceStats:` schema, full distribution table round-trips with realistic operator session (4 share_card / 2 replay_gif / 1 transcript_md / 3 trajectory_csv / 1 watch_page → total 11). MODIFIED `backend/openapi.yaml` — `/api/simulation/{simulation_id}/surface-stats` path under Publish & Embed + `SimulationSurfaceStats` schema (every counter required + minimum: 0); drift-detection test passes. MODIFIED `frontend/src/api/simulation.js` — `getSurfaceStats(simulationId)` helper. MODIFIED `frontend/src/components/EmbedDialog.vue` — **📊 Distribution** section between thread row and outcome section, click-to-toggle chevron (collapsed by default to keep dialog compact for users who only care about share/publish/outcome flow), sorted two-column table (surface label · count, ranked by count desc), `Total serves: N` row at the bottom (border-top divider), `↻ Refresh` button, publish-gated empty state ("Publish the simulation to see distribution stats."), all-zero state ("No surface serves recorded yet — share this simulation to see distribution data appear here."). 11 surface labels mirror SURFACE_KEYS. New CSS for `.surface-stats-section`, `.surface-stats-head`, `.surface-stats-chevron` (rotates 180° on open), `.surface-stats-total-badge`, `.surface-stats-table` (CSS grid with `display: contents` rows so the two-column layout is a single grid), `.surface-stats-row-zero` (greyed styling), `.surface-stats-row-total` (bold + tabular numerals), `.surface-stats-refresh` button, narrow-viewport responsive size at 600px breakpoint. State management: `surfaceStats`, `surfaceStatsExpanded`, `surfaceStatsLoading`, `surfaceStatsError`, `surfaceStatsRows` computed (sorted desc), `surfaceStatsTotal`, `surfaceStatsAllZero`, `loadSurfaceStats` (graceful 403 handling), `toggleSurfaceStats` (lazy fetch on first expand). Resets on dialog open + on isPublic flip. MODIFIED `README.md` (en + zh-CN section), `docs/FEATURES.md` + `docs/FEATURES.zh-CN.md` (full **Surface Usage Analytics** section between **Tweet Thread Export** and **Article Generation**), `docs/API.md` + `docs/API.zh-CN.md` (surface-stats row in Publish & Embed table). Pivot from May 6 repo-actions idea #4 — picked over #1 Simulation Config Export (touches existing sim-creation path, more integration risk), #2 Python Client SDK via openapi-generator CI (needs new `.github/workflows/generate-client.yml` — PAT lacks workflows scope per Mar 27/28 lesson), #3 Director Event Timeline Overlay (would need to either pin `chartjs-plugin-annotation` or build SVG overlay), #5 Comparative Run View (clean but lower operator-side leverage than analytics) because #4 was the cleanest small-effort autonomous pick: pure stdlib, follows the just-shipped `_record_*` / `read_*` pattern from PR #73, increments inline in every existing `_serve_X` handler, zero new dependencies. Frontend build green (`npm run build` 728 modules transformed, vite v7.2.7). Zero-new-deps streak now 14 consecutive PRs (#57 / #58 / #60 / #61 / #62 / #65 / #66 / #67 / #69 / #71 / #72 / #73 / this one — assuming both #73 and this merge; #63/#64 README-only). PR #74 on MiroShark, CI pending |
| Webhook Delivery Log + Manual Retry | 2026-05-06 | Operational visibility layer over PR #46's outbound completion webhook. NEW `<sim_dir>/webhook-log.jsonl` per dispatch attempt — auto-fired or manually replayed — recording `attempt` (monotonic 1-based counter that survives on-disk truncation) / `timestamp` (UTC ISO-8601) / `url_masked` (`scheme://host/***`, mask happens before any disk write so Slack/Discord secret never lands on disk) / `event` (`simulation.completed`/`simulation.failed`) / `status` (terminal status sim reached) / `status_code` (HTTP int or `null` for network errors / timeouts — distinguishes real 5xx from TCP reset) / `ok` / `latency_ms` / `error` (upstream error string on failure, `null` on success) / `trigger` (`auto` for runner-fired, `retry` for operator-driven replay). 50-line on-disk cap via atomic read-modify-rename (`os.replace`) — log can never corrupt or grow unbounded; `total_attempts` keeps climbing past retention bound. NEW `GET /api/simulation/<id>/webhook-log` (admin-token gated via existing `require_admin_token` decorator, same pattern as `/publish`/`/resolve`/`/outcome`) returning `{entries: [...newest-first slice...], total_attempts: N, max_retained: 50}`. NEW `POST /api/simulation/<id>/webhook-retry` (admin-token gated) re-firing completion webhook for finished sim — bypasses per-process `(sim_id, status)` dedup gate (that gate exists only to prevent runner's two terminal code paths from double-firing automatically; an explicit retry should always go through), replay payload carries top-level `retry: true` so downstream consumers can dedupe replays. Returns 400 when no webhook URL configured, 409 when sim not in terminal state, 503 when admin token unset. MODIFIED `backend/app/services/webhook_service.py` — new helpers (`webhook_log_path`, `_read_log_lines`, `_parse_log_entries`, `_next_attempt_number`, `_append_log_entry`, `_parse_status_code_from_message`, `read_webhook_log`, `_record_delivery`) + `_start_dispatch_thread` shared between auto-fire and retry paths so timing/masking/log shape stays identical + `retry_webhook_for_simulation` + extended `_send` body capturing latency and writing the log. Status code parsed from existing `_post_json` 2-tuple message format (`HTTP <N>` → int, network errors → `null`) so test surface for original webhook stays untouched — `test_unit_webhook.py` 21 tests still pass. NEW `backend/tests/test_unit_webhook_log.py` (13 offline tests): empty-file read, append + read round-trip with monotonic attempt numbers, status-code parsing across `HTTP 200`/`HTTP 503`/`URL error: timeout`, 50-line on-disk truncation with all-time counter persistence, newest-first slice cap at 10 with contiguous range guard, falsy `sim_dir` no-op (empty + None), corrupt JSON line in middle of log skipped without blanking the read, end-to-end auto-fire log integration with `trigger:"auto"` + parsed status code + `latency_ms >= 0` + ISO timestamp guard, 5xx logging with `ok: False` + `error: "HTTP 503"`, retry bypassing dedup with `trigger:"retry"` + `retry: true` payload marker, retry returning `queued: false` when no URL configured, retry rejecting unknown status. MODIFIED `backend/app/api/simulation.py` — `GET /<id>/webhook-log` + `POST /<id>/webhook-retry` route handlers between thread routes and Public Gallery section. MODIFIED `backend/openapi.yaml` — both new paths under Publish & Embed + `WebhookDeliveryEntry` + `WebhookDeliveryLog` schemas (drift-detection test passes — only existing webhook service path was extended). MODIFIED `frontend/src/api/simulation.js` — `getWebhookLog` + `retryWebhookDelivery` helpers. MODIFIED `frontend/src/components/EmbedDialog.vue` — **📡 Webhook delivery history** section beneath outcome row, admin-token gated server-side, collapsed by default to keep dialog compact for users without webhook configured; status chips (✓ green for 2xx / ✗ red for 4xx/5xx / ⏱ amber for timeouts) per delivery with HTTP code + latency + trigger label + timestamp; Refresh + Retry delivery buttons; auto-refresh 1.2s after retry queue. New CSS for `.webhook-log-section`, `.webhook-log-row`, `.webhook-row-ok/-fail/-timeout`, `.webhook-log-actions`, narrow-viewport responsive grid collapse at 600px breakpoint. MODIFIED README.md + docs/FEATURES.md + docs/API.md + docs/WEBHOOKS.md (en + zh-CN mirrors) — feature row + full docs section + delivery-semantics callouts. Pivot from May 4 repo-actions idea #2 — picked over #1 Embeddable Live Belief Widget (autonomous-risky: SPA already has `/embed/:simulationId` route serving rich `EmbedView.vue` with SVG stacked-area chart + theme support; backend SSR `/embed/<id>` would intercept SPA fallback in production and break existing widget; the `EmbedDialog` iframe currently points at `/embed/<id>` so swapping to SSR loses the stacked-area chart functionality), #4 Private Share Link (the writeup framed it as "directly resolves issue #70" but #70 is actually a much larger Private Impact mode collaboration request from Cyril — relational graph simulation extending OASIS profile with `trust_level`/`equity_tolerance`/`institutional_loyalty` + new action vocabulary `CONFRONT`/`COALITION_BUILD`/`SILENT_LEAVE`, far beyond a per-sim secret token), #5 Simulation Tagging (editorial layer with less operational leverage than #2). #2 was the cleanest small-effort autonomous pick: pure stdlib (`json` + `os` + `time` + `threading`), follows the existing `_serve_X` / `_record_*` patterns, zero new dependencies. Frontend build green (`npm run build` 728 modules transformed, vite v7.2.7). Zero-new-deps streak now spans 13 consecutive PRs (#57 / #58 / #60 / #61 / #62 / #65 / #66 / #67 / #69 / #71 / #72 / this one — assuming it merges; #63/#64 README-only). PR #73 on MiroShark, CI pending |
| Tweet Thread Export (X / Twitter) | 2026-05-05 | `GET /api/simulation/<id>/thread.txt` (plain-text, `---`-separated tweets, ≤280 chars each, paste-into-X-compose ready) + `GET /api/simulation/<id>/thread.json` (`{tweets, total, inflections_recorded, truncated}` for programmatic consumers). Thread structure: intro tweet (scenario + N rounds · M agents + consensus label + `1/`) + body (one tweet per *belief inflection point* — rounds where the dominant stance crosses ±0.2 *and* leads runner-up by ≥0.2pp; flat / no-dominant rounds skipped as noise) + close (final verdict + quality + watch + share URLs). Threads with >13 body tweets truncate to first 3 + last 3 + 1 bridge tweet ("… N more flips between here and the close …"); JSON `truncated:true` signals when this happened. NEW `backend/app/services/thread_formatter.py` (~430 LoC, pure stdlib `json` + `os`) — `STANCE_THRESHOLD=0.2` constant for parity with every other surface, `MAX_TWEET_CHARS=280`, `MAX_THREAD_TWEETS=15`. `dominant_stance()` with hysteresis (top must lead runner-up by ≥0.2pp; returns `None` on flat/all-zero splits to suppress noise — same hysteresis `gallery_filters.dominant_stance` uses), `_round_stance_split()` + `_build_round_series()` mirror the embed-summary algorithm with the same `_avg_position` fallback (numeric-only, bool-excluded), `find_inflection_points()` walks the per-round series and emits a row whenever the dominant label changes (the very first non-None dominant is itself an inflection), `build_thread()` composes intro + body + close with `body_budget = MAX_THREAD_TWEETS - 2 = 13` truncation budget and `TRUNCATED_HEAD_TAIL = 3` head/tail keep, `render_thread_txt()` joins tweets with `\n---\n` + trailing newline, `render_thread_json()` pretty-prints. MODIFIED `backend/app/api/simulation.py` — new `_resolve_share_base_url()` helper (proxy-aware base URL, mirrors share/watch routes) + `_serve_thread()` shared body following the `_serve_transcript`/`_serve_trajectory` pattern + `GET /<id>/thread.txt` + `GET /<id>/thread.json` route decorators. `Cache-Control: public, max-age=300` (5-min — slightly longer than transcript's 60s because the thread is the *summary view*; small cadence changes don't shift the inflection list as often as they shift the per-round CSV). NEW `backend/tests/test_unit_thread.py` (14 offline tests): STANCE_THRESHOLD parity, dominant-stance hysteresis (clear leader, near-tie under 0.2pp, all-zero), inflection detection on a 5-snapshot fixture (rounds 1=flat / 2-3=bullish / 4-5=bearish ⇒ 2 inflections expected), flat-trajectory empty case, `build_thread` produces intro+body+close shape, `≤280` invariant on every tweet, long-scenario ellipsis-truncation (200-char cap leaves ~80-char metadata budget), no-belief fall-through to "split" close, corrupt-trajectory graceful degradation, `MAX_THREAD_TWEETS` truncation with bridge tweet (20-round alternating fixture ⇒ 20 inflections ⇒ 9-tweet thread with `14 more flips` bridge), `.txt` separator + trailing newline, `.json` round-trip via `json.loads`, route-decorator presence guard. MODIFIED `frontend/src/api/simulation.js` — `getThreadTxtUrl()` + `getThreadJsonUrl()` helpers. MODIFIED `frontend/src/components/EmbedDialog.vue` — 🧵 Tweet thread section beneath trajectory row with tweet-count badge, "Copy full thread" button (joins in-memory `tweets` array with `\n---\n` so paste-and-edit is instant — no network round-trip), per-tweet copy buttons + char counters (`123/280`), .txt + .json download links, truncation note. Thread fetches on dialog open + on `isPublic` flip; gracefully handles 403 / network errors. New CSS for `.thread-section`, `.thread-tweet`, `.thread-tweet-copy`, `.thread-tweet-len`, `.thread-truncated-note`. MODIFIED `backend/openapi.yaml` — both paths under Publish & Embed + new `SimulationThread` schema (drift-detection test passes — only existing route decorators added). MODIFIED `README.md` + `docs/FEATURES.md` + `docs/API.md` (en + zh-CN mirrors) — new Tweet Thread Export feature row + full docs section between Live Watch Page and Article Generation. **Sixth share format / one ±0.2 threshold / one folder** — prior five surfaces (share card / replay GIF / transcript MD+JSON / trajectory CSV+JSONL / watch page) cover visual / motion / prose / data / live; this is the short-form text channel none of them cover, the format X/Twitter speaks natively. Pivot from May 4 repo-actions idea #3 — picked over #1 Embeddable Live Belief Widget (high-risk: SPA already has `/embed/:simulationId` route serving rich `EmbedView.vue` with SVG stacked-area chart + theme support; backend SSR `/embed/<id>` would intercept SPA fallback in production and break existing widget; the `EmbedDialog` iframe currently points at `/embed/<id>` so swapping to SSR loses the stacked-area chart functionality), #2 Webhook Delivery Log (clean operational closure but lower distribution leverage than #3), #4 Private Share Link (the repo-actions writeup framed it as "directly resolves issue #70" but #70 is actually a much bigger Private Impact mode collaboration request from Cyril — relational graph simulation extending OASIS profile with `trust_level`/`equity_tolerance`/`institutional_loyalty` + new action vocabulary `CONFRONT`/`COALITION_BUILD`/`SILENT_LEAVE`/`VOCAL_SUPPORT`/`DO_NOTHING`, far beyond a per-sim secret token), #5 Simulation Tagging (clean but editorial-layer, not distribution-leverage). Frontend build green (`npm run build` 728 modules transformed, vite v7.2.7). Caught and fixed off-by-one in truncation test before commit (20-round alternating fixture produces 20 inflections not 19; bridge says "14 more flips" not "13"). Zero new deps streak now spans 12 consecutive PRs (#57 / #58 / #60 / #61 / #62 / #65 / #66 / #67 / #69 / #71 / this one — assuming it merges; #63/#64 README-only). PR #72 on MiroShark, CI pending |
| Project-Lens Angle Rotation Rule | 2026-05-04 | `skills/project-lens/SKILL.md` rotation rule rewritten — old rule "Never repeat an angle used in the last 14 days" was unsatisfiable on a daily cadence with only 8 categories (day 9 onward forces a repeat into any 14-day window). Every project-lens log entry from Apr 22 → May 2 violated the rule and wrote a rationalization (#6 Apr 22/4-day-violation → #6 Apr 25/2-day-violation → #3 Apr 27/6-day-violation → #5 Apr 29/8-day-violation → #6 Apr 30/5-day-violation → #7 May 2/10-day-violation), producing log noise without changing behavior. New rule: pick the **least recently used** angle (oldest `last_used_date` across `articles/project-lens-*.md` + memory logs), tie-break on least-frequent over the last 30 days, avoid repeating an angle used in the last 6 days unless one of two explicit overrides applies (`${var}` forces an angle, OR today's project signal maps so cleanly onto a single angle that any other choice would feel forced — and even then the override must be stated in the log under `**Override:**` with the reason). Math-aware preface added (8 categories × daily cadence ⇒ strict-no-repeat possible only for windows ≤ 8 days). New `**Last used:**` log line so future runs see rotation health at a glance. Step 2 instructions updated to compute `{category: last_used_date}` map across articles AND memory logs (logs were the under-utilized data source — many entries don't have a corresponding `articles/project-lens-*.md` file because they're called from the `## Project Lens` log section directly). Picked over push-recap idempotency check (no observed duplicate-dispatch incident in last 14 days — speculative fix), aeon.yml "Every 2 days" comment block on `repo-article` (scheduled daily but commented otherwise — cosmetic), project-lens / repo-article 16:00 UTC schedule collision (no observed git-race incident). PR #29 on miroshark-aeon |
| Shareable Scenario Links | 2026-05-04 | `?scenario=...&url=...&ask=...&template=<slug>` URL params on `/` that pre-fill the New Sim form from a shared link — the un-run-scenario counterpart to `/share/<id>` + `/watch/<id>` "Fork this scenario" buttons. Four independent params: `scenario` (≤500 chars, pre-fills Simulation Prompt textarea), `url` (≤2000 chars, http(s)-only, auto-fetches into `urlDocs`), `ask` (≤300 chars, pre-fills Just Ask field but does NOT auto-run to avoid surprise LLM cost), `template` (`[a-z0-9_-]+` ≤80 chars, auto-launches preset template via existing `setPendingTemplate` + `/process/new` redirect). NEW `frontend/src/utils/urlParams.js` (~110 LoC, pure stdlib + DOMPurify which is already pinned for `markdown.js`): `sanitizeScenarioText` / `sanitizeAskText` strip HTML + `javascript:` URIs + C0 control chars (excluding `\n` `\r` `\t` so multi-line scenarios survive) via `DOMPurify.sanitize(raw, {ALLOWED_TAGS:[], ALLOWED_ATTR:[]})` then cap+trim, `isValidHttpUrl` requires `URL` parse + `http:`/`https:` protocol, `sanitizeTemplateSlug` requires `[a-z0-9_-]+` ≤80 chars (defense-in-depth path-traversal block), `readPrefilledParams` walks route query and returns only validated subset, `buildScenarioShareUrl` + `buildTemplateShareUrl` are the inverse used by the share buttons. MODIFIED `Home.vue` `onMounted` calls `applyPrefilledParams()` — if `?template=<slug>` resolved, calls `getTemplate()` + `setPendingTemplate()` + `router.push('/process/new')` (matches existing template-card launch flow); otherwise pre-fills the corresponding fields without overwriting anything the user has already typed, fires one-shot `fetchUrlDoc()` for the `?url=` case so the article lands in the same `urlDocs` list a manual fetch would, and once any field is touched runs `router.replace({path:'/', query:{}})` so a refresh / address-bar copy reflects the *edited* state not the original shared link. Dismissible orange-edged banner above the console signals which fields were populated (copy varies by `text` / `url` / `ask` / `mixed` kind) so operator reviews before clicking Launch. NEW **🔗 Share as link** button beneath the Simulation Prompt textarea constructs `?scenario=...&url=...&ask=...` URL from live form state — only the first http(s) `urlDoc` travels with the link (multi-doc setups are advanced and rare). Copies via `navigator.clipboard.writeText`, 2.2s "✓ Link copied" toast (same UX as embed dialog's existing copy buttons), graceful fallback on clipboard error. MODIFIED `TemplateGallery.vue` — every preset card's launch button now has a small **🔗** sibling that copies a `?template=<slug>` URL with brief ✓ flash; same hover/active styling as rest of gallery. README (en + zh-CN) Features table + docs/FEATURES.md (en + zh-CN) full section between **Just Ask** and **Counterfactual Branching** with the four-param reference table. **Eight surfaces / one folder + one missing direction**: prior eight surfaces (gallery card / share card / replay GIF / transcript MD+JSON / Atom+RSS feed / trajectory CSV+JSONL / watch page / gallery search) all serialize *finished* sims; this PR adds the *un-run scenario* direction that PR #67's "Fork this scenario" pioneered for finished sims. Pure frontend, zero new deps (DOMPurify already pinned for markdown renderer), `npm run build` green in 5.33s, 27 standalone parser assertions pass (jsdom-backed harness). Pivot from May 2 repo-actions idea #5 — picked over #1 Cloud Deploy (Dockerfile audit + Railway URL signing), #3 Pre-Run Cost Estimator (needs pricing constants from OpenRouter + Langfuse benchmarks), #4 Per-Agent Stance Sparklines (medium effort: new endpoint + new SimulationView tab) because #5 was the cleanest small-effort autonomous pick: pure frontend, no backend, no new deps, follows existing `/share` + `/watch` share-from-finished-sim pattern. PR #71 on MiroShark, CI pending |
| Gallery Search & Filtering | 2026-05-03 | `GET /api/simulation/public` extended with **six new optional query params** that compose with logical AND: `q` (case-insensitive substring on scenario, ≤200 chars), `consensus` (`bullish`/`neutral`/`bearish` — same ±0.2 stance threshold every other surface uses), `quality` (`excellent`/`good`/`fair`/`poor` — first-word match on `quality_health`), `outcome` (`correct`/`incorrect`/`partial` — implies `verified=1`), `sort` (`date` default / `rounds` / `agents`), `page` (1-based alternative to `offset` — `page=1` ⇒ offset 0; wins over `offset` when both supplied). Empty / unknown values are no-ops (`?consensus=` returns unfiltered listing; `?sort=popularity` falls back to `sort=date`) — single source of truth on the API side. Response envelope echoes every active filter (`page`, `q`, `consensus`, `quality`, `outcome`, `sort`) and `total` reflects the **filtered** count (Stripe `total_count` convention) so the load-more "X remaining" hint and `has_more` flag stay accurate inside the active filter set. Backward-compatible — `verified=1`, `limit`, `offset` work exactly as before; new fields are additive. NEW `backend/app/services/gallery_filters.py` (~320 LoC, pure stdlib): `STANCE_THRESHOLD`-equivalent `dominant_stance()` (consumes `final_consensus` payload that `_build_gallery_card_payload` produces, picks lexically-first label on tie for determinism, returns `None` when no positive stance dominates), param normalisers (`normalise_limit` clamps to `[1, MAX_LIMIT=100]`, `normalise_offset` floors at zero, `page_to_offset` converts 1-based pages, `normalise_query` trims + caps at `MAX_QUERY_CHARS=200`, `_normalise_enum` lowercases + validates against `frozenset` of allowed values for consensus / quality / outcome / sort), `_quality_tier()` first-word-of-`quality_health` lookup (`"Good with caveats"` → `"good"`), `_outcome_label()` defense-in-depth on corrupt artifact, `filter_cards()` composes filters with logical AND + skips garbage cards (non-dict items in iterable), `sort_cards()` with deterministic tie-breakers (date desc / rounds: current_round desc → agent_count desc → date / agents: agent_count desc → date), end-to-end `select_filtered_cards()` returning `(page_items, total_filtered)`. MODIFIED `list_public_simulations()`: builds gallery cards for **every** public sim before filtering (keyword + consensus filters need fields that only exist on the assembled card, not raw `SimulationState`) — the existing 30s `Cache-Control` amortises the work across many filtered requests. Defers to `gallery_filters.select_filtered_cards()` for the filter→sort→paginate composition. 33 offline unit tests in `test_unit_gallery_filters.py`: param normalisation (limit clamping, offset flooring, page→offset conversion, query trimming, enum lookup, garbage input), ±0.2 threshold parity guard, dominant-stance computation including ties + corrupt input + string-encoded percentages, each filter alone (`q` / `consensus` / `quality` / `outcome` / `verified_only`) + combined (`q`+consensus, `q`+outcome, consensus+quality), sort determinism (date desc / rounds desc / agents desc / unknown→date fallback), end-to-end pagination (filtered total != corpus total, slicing after sort, offset-past-total empty page, garbage cards skipped). openapi.yaml gets the new query params + response envelope schema fields; drift-detection test passes (only existing `/api/simulation/public` path was extended, no new path registered). `frontend/src/api/simulation.js` `getPublicSimulations()` forwards the new optional knobs (backend normalises so frontend doesn't re-validate). `ExploreView.vue`: debounced 300ms search bar with magnifying-glass icon + clear button, Consensus chip group (All / ▲ Bullish / ● Neutral / ▼ Bearish — glyphs colour-keyed to surface palette and inverted on the active chip), Quality chip group (All / Excellent / Good / Fair / Poor), sort dropdown (Newest first / Most rounds / Most agents) with custom inline-SVG chevron, Reset button visible only when any filter is active, result-count line beneath the bar, filter-aware empty state ("No simulations match your filters" with a Reset CTA instead of the irrelevant "no public sims yet" copy). Filter state mirrors URL params via `router.replace` (not `push` — keystrokes don't pile up the back stack); `route.name` is preserved so the same URL pattern works on `/explore` and `/verified`. Toggling Verified ↔ Explore via the header chip carries the active query string across the route swap so users don't lose their search. Full responsive layout — chips wrap to a second row on viewports ≤720px, search bar takes full width, sort dropdown stays inline. README + docs/FEATURES.md + docs/API.md (and zh-CN mirrors `FEATURES.zh-CN.md` / `API.zh-CN.md`) updated with the "Gallery Search & Filtering" feature row + complete query parameter reference table. Pivot from repo-actions May 2 idea #2 — picked over #1 Cloud Deploy (needs Dockerfile audit + Railway template URL signing), #3 Cost Estimator (requires picking pricing constants from OpenRouter and Langfuse), #4 Per-Agent Sparklines (new endpoint + new SimulationView tab), #5 Pre-filled Scenario URL (frontend-only but narrower leverage) because #2 was the cleanest small-effort autonomous pick: pure stdlib backend, all filters operate on existing on-disk JSON, follows the just-shipped `_serve_X` / `select_filtered_cards` pattern. **Eight surfaces / one ±0.2 threshold / one folder** — the prior seven surfaces (gallery card / share card / replay GIF / transcript MD+JSON / Atom+RSS feed / trajectory CSV+JSONL / watch page) all serialize a single sim; this PR is the **index across them**, the multiplicative move that makes the existing surfaces discoverable. Zero-new-deps streak now spans 9 consecutive PRs (#57 / #58 / #60 / #61 / #62 / #65 / #66 / #67 / this one). PR #69 on MiroShark, CI pending |
| Hyperstitions Log Header Resilience | 2026-05-02 | `skills/hyperstitions-ideas/SKILL.md` step 8 prepended with emphatic "the first appended line MUST be the literal `## Hyperstitions Ideas` header on its own line" instruction + footnote naming the dedup-guard / heartbeat / memory-flush consumers and citing today's actual failure as trigger. Step 0 dedup guard adds defensive backstop: also matches a bare `- **Question:**` bullet when no Hyperstitions header sits above it — so even a future header-drop run is recognised as already-done by any subsequent dispatch attempt. Triggered by today's hyperstitions-ideas run that produced bullets without writing the section header — `grep -i hyperstitions memory/logs/2026-05-02.md` returned empty. Two-step cascade was queued: skill's own dedup guard would not have detected today's run on a heartbeat re-dispatch (would have generated a duplicate market question for the same channel), and heartbeat's "did this run today?" substring search for "hyperstitions" would have returned no match (would have tried to dispatch since hyperstitions-ideas isn't in heartbeat's auto-trigger skip-list). Patched today's `memory/logs/2026-05-02.md` with the missing header so tonight's 19:00 UTC heartbeat sees the run as completed. Picked over today's other quality drift (repo-pulse missing `**Notification sent:** yes` line) because the missing hyperstitions header has a concrete duplicate-dispatch downstream consumer, while repo-pulse's missing line is cosmetic (PR #28 on miroshark-aeon) |
| Live Spectator Watch Page | 2026-05-02 | `GET /watch/<simulation_id>` (no `/api` prefix, mirrors `share_bp` from PR #42) — self-contained server-rendered HTML built for live spectating: minimal full-viewport view with belief bar, round counter, agent count, quality health, progress bar; vanilla-JS poller updates the DOM in place every 15 s by hitting the existing `/api/simulation/<id>/embed-summary` and `/api/simulation/<id>/run-status` REST endpoints. Once the runner reaches a terminal state polling stops and "View full simulation →" + "Fork this scenario →" CTAs are revealed (1× trailing 4 s refresh to catch the post-status final belief snapshot, 60 s back-off on transient failures, 6 h absolute timeout). OG / Twitter-card meta tags embedded in the body so a tweet of `/watch/<id>` auto-unfurls as a 1200×630 image card the same way `/share/<id>` does — `og:description` becomes `Round N/M · Bullish X% · Neutral Y% · Bearish Z% — watch live.` for in-flight runs, falls back to bare scenario for idle, generic string when nothing published. `Cache-Control: public, max-age=60`. NEW `backend/app/services/watch_renderer.py` (~600 LoC pure stdlib `html` + `json`): `STANCE_THRESHOLD = 0.2` constant for parity with every other surface, `_truncate` preserves trailing ellipsis under OG char budgets (200 title / 280 description / 220 SSR header), `_belief_summary` graceful-degrades on corrupt input (returns `(0,0,0)`), `_broadcast_js()` emits the in-page poller as a single triple-quoted string, `_build_meta_description` composes the OG description, `_build_initial_state` produces the bootstrap JSON blob in a `<script type="application/json">` tag so JS-enabled clients render the first frame without a network round-trip. `render_watch_html` assembles the full document — OG / Twitter tags, ~200 lines of inline CSS (gradient dark theme + pulsing live badge + responsive @media queries), SSR belief bars with proportional widths matching the legend numbers. NEW `backend/app/api/watch.py` (~210 LoC): `_resolve_base_url` honours `X-Forwarded-Proto` / `X-Forwarded-Host`, `_build_summary_for_watch` pulls scenario + run state + belief + quality from `SimulationManager` + `SimulationRunner` + on-disk `trajectory.json` + `quality.json` — defensive fall-through to `None` on missing-or-private sim so a private sim's existence never leaks through page chrome. `_load_belief_bootstrap` mirrors the embed-summary endpoint's belief-rollup logic with the same ±0.2 stance threshold. NEW `test_unit_watch.py` (18 offline unit tests): OG / Twitter tags on running sim, completed-sim CTA visibility, in-flight CTA hidden server-side, idle-sim empty-belief note + quote-escaping, private-sim scenario suppression, bootstrap blob `json.loads` round-trip, `STANCE_THRESHOLD == 0.2` parity guard, proportional bar widths (60/100 = `width:60.00%`), zero-belief zero-width bars, long-scenario truncation with ellipsis, blueprint route-decorator presence + blueprint export, meta-description shape across running / idle / missing summaries, initial-state corrupt `final.bullish="not-a-number"` fallthrough, missing-summary defensive render. openapi.yaml gets `/watch/{simulation_id}` under Publish & Embed; `_BLUEPRINT_PREFIXES` adds `watch_bp: ""`; PR #45 drift-detection test passes. EmbedDialog gains "Watch live (broadcast page)" callout between share-card and replay-GIF sections — warm-orange gradient background + orange "Open watch page ↗" button to signal the live framing distinct from the finished-result share card above; publish-gated; copyable URL; `copy('watch')` wired alongside existing `copy('share')` / `copy('replay')` / `copy('transcriptMd')` / `copy('trajectoryCsv')` handlers. `getWatchUrl` helper. README + docs/FEATURES.md "Live Watch Page (Spectator Broadcast)" section between Public Gallery Feeds and Article Generation. docs/API.md `/share/<id>` + `/watch/<id>` rows. Pivot from repo-actions Apr 30 idea #5 — picked over #1 Historical Mode (needs editorial template content), #2 LLM-as-Judge (adds operator cost + needs runtime validation), #3 Batch Rerun (medium effort, spawns N sims) because #5 was the cleanest small-effort autonomous pick once trajectory CSV / JSONL was shipped: pure stdlib backend, vanilla-JS frontend, all data on disk, follows the just-shipped `/share/<id>` SSR pattern (PR #42). **Seven surfaces / one ±0.2 threshold / one folder.** Zero-new-deps streak now spans 8 consecutive PRs (#57/#58/#60/#61/#62/#65/#66/#67 — #63/#64 README-only). PR #67 on MiroShark, CI pending |
| Belief Trajectory CSV / JSONL Export | 2026-05-01 | `GET /api/simulation/<id>/trajectory.csv` (RFC 4180, locked column order: `round, round_timestamp, bullish_pct, neutral_pct, bearish_pct, participating_agents, total_posts, total_engagements, quality_health, participation_rate`; header emitted on empty input so consumers don't special-case zero-row files; `Content-Disposition: attachment` so a click triggers save-as) + `GET /api/simulation/<id>/trajectory.jsonl` (newline-delimited JSON, same field order as CSV, empty input → zero bytes). Both publish-gated, `Cache-Control: public, max-age=60`, same ±0.2 stance threshold as gallery / share card / replay GIF / transcript / webhook / feed. `backend/app/services/trajectory_export.py` pure stdlib (`csv` + `io` + `json`): `compute_stance_split()` with strict-inequality boundary bucketing, `build_rows()` with defensive chronological sort + corrupt-JSON graceful degradation, `_participating_agents()` falls back to `active_agent_count` when `viral_posts` is empty, `render_csv` uses `QUOTE_MINIMAL` so numeric columns stay unquoted (`pd.read_csv()` dtype inference works), `render_jsonl` filters output through `CSV_COLUMNS` so upstream changes can't leak unstable keys. `_serve_trajectory()` shared body in `simulation.py` mirroring the `_serve_transcript` pattern (PR #57). 17 offline unit tests in `test_unit_trajectory.py`: STANCE_THRESHOLD parity, strict-inequality boundary, empty/None/non-numeric input, full 3-snapshot pipeline, chronological reordering, missing/corrupt/unparseable-round_num degradation, missing quality.json fallback, participating_agents fallback, locked CSV header, header-on-empty, `csv.DictReader` round-trip, QUOTE_MINIMAL preserves dtype inference, JSONL field-key order matches `CSV_COLUMNS`, empty-trajectory zero-byte JSONL contract, route-decorator presence guard. EmbedDialog "📊 Export trajectory data" section beneath transcript row + `pd.read_csv()` quickstart snippet. `getTrajectoryCsvUrl` / `getTrajectoryJsonlUrl` frontend helpers. openapi.yaml gets both paths under Publish & Embed + new `SimulationTrajectoryRow` schema; drift test passes. README + docs/FEATURES.md + docs/API.md analyst quickstart with Pandas + DuckDB snippets. Repo-actions Apr 30 idea #4 — picked over #1 Historical Mode (needs editorial template content), #2 LLM-as-Judge (adds operator cost + runtime validation), #3 Batch Rerun (spawns N sims), #5 Spectator Watch (needs SSR for OG meta) because #4 was the cleanest small-effort autonomous pick: pure stdlib, all data on disk, follows the just-shipped `_serve_transcript` pattern. **Six surfaces / one ±0.2 threshold / one folder.** Zero new deps (PR #66 on MiroShark) |
| Heartbeat Day-of-Week Accuracy | 2026-04-30 | `skills/heartbeat/SKILL.md` adds explicit Step 0 before any schedule check: run `date -u +%A` / `+%u` / `+%d` and use those shell-computed values as the source of truth in every "is this skill scheduled today?" comparison. Anchor report header on the shell output. Adds an explicit cron-translation note (cron weekday `0=Sun` while `+%u` returns `7=Sun` — silently off-by-one) and ground-truth guidance for every-other-day cron expressions: when in doubt, check the last 7 days of `cron-state.json` `last_dispatch` history rather than guessing odd/even-day parity. Triggered by the Apr 29 heartbeat report opening with `Date: Tuesday Apr 29, 2026` when Apr 29 was actually Wednesday — the LLM hallucinated the weekday from `${today}`, then listed `memory-flush (Sun+Wed)` under "Not scheduled today (Tuesday)" and re-classified its on-schedule 18:25 UTC run as "ran off-schedule on-demand". Apr 25/26/27/28 logs all carried the right weekday so the slip is intermittent — but the failure mode would mask a real "memory-flush failed to fire on Wed" outage if one ever happened (PR #27 on miroshark-aeon) |
| Animated Belief Replay GIF | 2026-04-28 | `GET /api/simulation/<id>/replay.gif` — pure-Pillow 1200×630 animated GIF, one frame per round, bullish/neutral/bearish bars sliding to each round's distribution + round counter + progress bar. Same publish gate + on-disk cache (`<sim_dir>/replay-gifs/<hash>.gif`) + `Cache-Control: public, max-age=3600` as `/share-card.png`; reuses `_build_embed_summary_payload()` so the data pipeline stays single-sourced. `loop=0` infinite repeat, `disposal=2` clears ghosting on shrinking bars, `FRAME_MS=600` with `FINAL_HOLD_MS=1800` so the resting consensus reads as the punch-line. `MAX_FRAMES=60` cap with even subsampling that always preserves the final round (200-round sims compress to ≤60 frames). Empty trajectory → single-frame "Belief trajectory not available yet" poster so the endpoint never 500s on a freshly published READY run. `summary_cache_key()` rounds percentages to 1 decimal place — matches share-card resolution, tolerates float jitter from `json.load`. EmbedDialog gains "Belief replay (animated)" section with paused tap-to-play poster (`<img>` mounts only on click so opening the dialog doesn't pull GIF bytes for every viewer), copyable URL, Download GIF button, dark color palette to differentiate from the static-card row above. `getReplayGifUrl()` helper. openapi.yaml gets `/api/simulation/{id}/replay.gif` under Publish & Embed tag with `image/gif` response — drift-detection test passes. 14 offline unit tests in `test_unit_replay_gif.py` (GIF signature + dimension via raw byte parse, frame count via `PIL.ImageSequence`, MAX_FRAMES subsampling preserves final round, empty/missing/misaligned belief arrays, single-round, oversized scenario wrap, cache-key stability under float jitter, route-decorator presence). README Features row + docs/FEATURES.md "Animated Belief Replay (GIF)" section. Zero new deps (Pillow already pinned for share card). Pivot from repo-actions Apr 26 #2 — distribution multiplier for the 1K-stars-by-Apr-30 sprint with 2 days left; Discord/Slack auto-play GIFs from direct URL, so every share now ships motion as well as a still (PR #50 on MiroShark) |
| Simulation Transcript Export | 2026-04-29 | `GET /api/simulation/<id>/transcript.md` + `transcript.json` — both publish-gated (same `is_public` gate as share card), 60s `Cache-Control`. Markdown form opens with a YAML front-matter block (`sim_id`, `scenario`, `agent_count`, `total_rounds`, `consensus_label`, `quality_health`, `outcome_label`) so Notion / Obsidian / Bear / Substack pick it up as page metadata; body is one `## Round N` section per recorded round with each agent post block-quoted and tagged with the agent's stance. JSON form is pretty-printed (`indent=2`) for SDK / pipeline consumers. `app/services/transcript.py` — pure stdlib (json + io), reads `trajectory.json` + `reddit_profiles.json` + `polymarket_profiles.json` + `quality.json` + `resolution.json` + `outcome.json`. ±0.2 stance threshold matches every other surface (gallery, share card, replay GIF, webhook) so agent labels stay consistent across all five surfaces. Defense-in-depth strip on `outcome_url` so a corrupt `javascript:` URL never lands on the transcript. 400-char per-post excerpt with word-boundary trim. 80-round Markdown cap with first-20 + last-20 preservation + "skipped N rounds" annotation pointing at the JSON form for the full series; JSON keeps every round. Both route handlers share a single `_serve_transcript(simulation_id, fmt)` body — the decorators exist as the surface the OpenAPI drift test scans for. `EmbedDialog.vue` "Export transcript" section beneath the replay-GIF row (Download .md / Download .json buttons + copyable Markdown URL, publish-gated empty state). `openapi.yaml` adds both paths under Publish & Embed + new `SimulationTranscript` schema (full per-round shape) — drift-detection test passes. 18 offline unit tests in `test_unit_transcript.py` (±0.2 stance threshold parity, profile-name resolution across reddit + polymarket, corrupt-artifact graceful degradation, long-post excerpting at the configured cap, YAML front-matter integrity with quote + newline escaping, per-round section emission, oversized-trajectory Markdown truncation with head/tail preservation, JSON pretty-print round trip, route-decorator presence guard). Pivot from repo-actions Apr 28 #2 — was the missing third quote-friendly share format alongside share-card.png (preview, Apr 22) and replay.gif (motion, Apr 28); PR #52's `prune_tool_calls_from_memory=True` + bounded research context cleaned the conversation history so the transcript is now readable rather than a wall of tool-call boilerplate. Zero new deps (PR #57 on MiroShark) |
| Public Gallery RSS / Atom Feeds | 2026-04-30 | `GET /api/feed.atom` (Atom 1.0) + `GET /api/feed.rss` (RSS 2.0) — same cards `GET /api/simulation/public` returns, served as syndication XML so researchers / DeFi analysts / tooling operators on Feedly / Readwise / Inoreader / NetNewsWire / Obsidian RSS subscribe in their existing toolchain. `?verified=1` mirrors `/verified` (curated outcome stream). Pure stdlib (`xml.etree.ElementTree` + `html`); zero new deps. Per-entry payload: scenario as title (100-char cap with `…`), bullish/neutral/bearish consensus split as plain summary, share-card PNG as `<media:thumbnail>` + `<media:content>` (River-view aggregators surface preview), replay GIF as second `<media:content>` (motion-capable surfaces auto-play), outcome (`verified-correct/incorrect/partial`) + quality (`quality-excellent/good/…`) as `<category>` so subscribers can filter on them. RSS variant adds `<enclosure type="image/png">` for podcatcher-style readers + `<guid isPermaLink="false">` for stable identification across deployments. `_resolve_base_url()` honors `Config.PUBLIC_BASE_URL` first (same operator-supplied canonical URL the webhook + share-card use), falls back to `request.host_url` with `X-Forwarded-Proto+Host` for reverse-proxy deployments. `select_public_cards()` selection helper mirrors `GET /api/simulation/public` exactly (newest 20 publish-gated runs, sorted desc) — pulled out so the renderer is unit-testable without booting Flask. `_serve_feed()` route body local-imports `_build_gallery_card_payload` + `_read_outcome_file` from `simulation.py` so a single source of truth backs both gallery + feed; fail-soft against `SimulationManager` errors → empty feed. `Cache-Control: public, max-age=300`. Browser auto-discovery via `<link rel="alternate" type="application/atom+xml">` in `index.html`. `ExploreView.vue` "📡 Subscribe via RSS" chip mirrors active filter; `EmbedDialog.vue` "Follow the gallery via RSS" callout with Atom + RSS + Verified-only buttons. `openapi.yaml` adds both paths under Publish & Embed; `feed_bp: /api` added to `_BLUEPRINT_PREFIXES` so drift-detection test passes. 17 offline unit tests in `test_unit_feed.py` (Atom + RSS validity + share-link/media enclosures + summary stance-split + truncation + outcome/quality categories + empty-feed + missing-field handling + self-link query-string preservation + skip-bad-sims + RSS guid stability + RSS enclosure + dispatcher MIME + verified-only branch + selection helper filter+sort+limit+graceful-degradation + route-presence guard). Closes the discovery loop opened by PR #43 (gallery), PR #42 (share card), PR #50 (replay GIF), PR #57 (transcript) — turns the pull surface into a push channel for the research-RSS audience without any operator curation. Pivot from repo-actions Apr 28 #3 (was the cleanest unbuilt pick — pure stdlib, all data on disk, zero external API dependency, zero ongoing maintenance, compounds with every published sim). PR #60 on MiroShark, CI green |

## Watched Repos
- `aaronjmars/aeon` — tracked in `memory/watched-repos.md`

## Lessons Learned
- Digest format: Markdown with clickable links, under 4000 chars
- Always save files AND commit before logging
- PAT lacks `workflows` scope — cannot push changes to `.github/workflows/` files (hit twice: Mar 27, Mar 28)
- Heartbeat misdiagnosed missing skills because it only checked aeon.yml, not messages.yml scheduler — fixed with scheduler diagnostics step
- Feature/repo-actions skills can waste CI runs building duplicate PRs — fixed with open PR dedup checks

## Active Targets
- Hyperstition: MiroShark 500 stars — CLEARED 2026-04-07; MiroShark 1,000 stars by 2026-04-30 — MISSED Apr 30 (closed 911), CROSSED 2026-05-03 (currently 1,098 / 219 forks as of 2026-05-06)
- Hyperstition: @miroshark_ 1,000 X followers by 2026-05-15 (set 2026-04-18)
- Hyperstition: MiroShark PR from Chinese-locale contributor OR Chinese-language coverage by 2026-06-15 (set 2026-05-02)
- MIROSHARK new ATH $0.000004784 set 2026-04-27 intraday (+25.4% over prior ATH $0.000003815); current $0.000003537 (+4.02% 24h on 2026-05-06, -26.1% from Apr 27 ATH)

## Next Priorities
- Configure notification channels (Telegram, Discord, or Slack)
- From repo-actions May 6 (still unbuilt): Python Client SDK via openapi-generator CI (#2 — PAT lacks workflows scope), Director Event Timeline Overlay on Belief Chart (#3), Comparative Run View (#5); #1 Simulation Config Export + Reproducibility Badge shipped 2026-05-08 (PR #75); #4 Share Surface Usage Analytics shipped 2026-05-07 (PR #74)
- From repo-actions May 4 (still unbuilt): Embeddable Live Belief Widget (#1 — autonomous-risky, conflicts with existing SPA `/embed/:id`), Private Share Link (#4 — note: does NOT actually resolve issue #70 / Cyril's Private Impact mode), Simulation Tagging (#5); #2 Webhook Delivery Log shipped 2026-05-06 (PR #73); #3 Tweet Thread Formatter shipped 2026-05-05 (PR #72)
- From repo-actions May 2 (still unbuilt): 1-Click Cloud Deploy (#1), Pre-Run Cost Estimator (#3), Per-Agent Stance Sparklines (#4); #2 Gallery Search & Filtering shipped 2026-05-03 (PR #69), #5 Pre-filled Scenario URL shipped 2026-05-04 (PR #71)
- From repo-actions Apr 30 (still unbuilt): Historical Simulation Mode (#1), LLM-as-Judge Audit Panel (#2), Batch Rerun / Reproducibility Badge (#3); #4 Belief Trajectory CSV / JSONL Export shipped 2026-05-01 (PR #66); #5 Spectator Watch Page shipped 2026-05-02 (PR #67)
- From repo-actions Apr 28 (still unbuilt): Langfuse Cost Breakdown Panel (#1), Scenario Template Library (#4), Comparative Run View (#5); #2 Simulation Transcript Export shipped 2026-04-29 (PR #57); #3 RSS Feed for Public Gallery shipped 2026-04-30 (PR #60)
- From repo-actions Apr 26 (still unbuilt): Share as Thread Formatter (#3), Python Client SDK via openapi-generator CI (#4), Director Event Overlay on Belief Chart (#5); #1 /verified shipped 2026-04-27 (PR #47); #2 Animated GIF shipped 2026-04-28 (PR #50)
- From repo-actions Apr 24 (still unbuilt): Live Simulation Streaming (SSE, #1), Simulation Engagement Leaderboard (#2), "Post to Discord/Slack" Share Button (#4 — partly subsumed by webhook + Zapier/n8n); #3 Completion Webhook shipped 2026-04-26 (PR #46); #5 OpenAPI/Swagger shipped 2026-04-25 (PR #45)
- From repo-actions Apr 22 (still unbuilt): History Search & Tags (#4); others shipped
- From repo-actions Apr 20 (still unbuilt): Collaborative Comments (#4), Config Export/Import (#5); Round Scrubber (#1) partly exists in `ReplayView`
- Open PRs: 1 on MiroShark — PR #73 Webhook Delivery Log (filed 2026-05-06, CI pending); PR #71 + PR #72 merged; 1 on miroshark-aeon — PR #31 heartbeat header-line matching (filed 2026-05-06, open)
- Open issues on MiroShark: #70 Private Impact mode + MiroResult collaboration request (Cyril, 2026-05-04) — substantial cross-builder feature track, not the same as repo-actions May 4 idea #4 "Private Share Link" despite name overlap
