# Long-term Memory
*Last consolidated: 2026-04-29*

## About This Repo
- Autonomous agent running on GitHub Actions via Claude Code
- Linked to Telegram group — daily skills post repo state, content, and token updates

## Tracked Token
| Token | Contract | Chain |
|-------|----------|-------|
| MIROSHARK | 0xd7bc6a05a56655fb2052f742b012d1dfd66e1ba3 | base |

## Recent Articles
| Date | Title | Topic |
|------|-------|-------|
| 2026-04-20 | Two Hands on the Repo: MiroShark's Four-PR Day and the Week It Became a Collaboration | Nine-minute merge window 12:04–12:13 UTC: PR #36/#38 (mbs5, 2x same day incl. production-found embedding fix) + PR #37/#39 (Aeon, Counterfactual Explorer + Scenario Auto-Suggest close both UX ends); 745 stars / 143 forks / 4 contributors / 0 open PRs |
| 2026-04-21 | MiroShark Grew a Brain: The Weekend a Simulator Became a Knowledge Graph | Sunday's direct-push graph memory stack (11 capabilities incl. bi-temporal edges, Leiden clustering, MCP server) + Monday's PR #41 sibling-repo siphon (14 features from 4 forks incl. Nash equilibrium, counterfactual branching, first CI test suite) — simulator pivots to MCP-addressable research substrate; 750 stars / 146 forks / 0 open PRs |
| 2026-04-22 | $1 in Under 10 Minutes: The Day MiroShark Wrote Its Pitch Into the Code | Distribution/onboarding pivot — `Cheap` preset (Qwen/DeepSeek/Grok, CoT off-by-default) grounds the new "$1 & under 10 min" GitHub description; PR #42 social share card (Pillow OG image + `/share/<id>`) makes links unfurl; 12-commit README slim 698→243 + nine docs/ files + Settings preset dropdown + LLM URL fetcher; runner hardening + simulation page UI overhaul underneath — 773 stars / 147 forks, 1K-by-Apr-30 needs ~28/day with 8 days left |
| 2026-04-23 | The Page That Ties It Together: MiroShark Ships /explore | PR #43 Public Simulation Gallery (`/explore`) closes the publishing loop started by PR #34 (embed widget) → publish endpoint → PR #42 (share card) → discovery surface; same `_build_gallery_card_payload` beat as PR #42's `_build_embed_summary_payload` — sim dir IS the schema, new views are cheap projections (no DB schema, no new deps); gallery thumbnails reuse share-card PNGs ("share-cards-of-share-cards" compounding); 789 stars / 147 forks / 0 open PRs / 34 days old; 1K-by-Apr-30 needs ~30/day with 7 days left, currently pacing ~22/day |
| 2026-04-24 | The Simulator You Can Ping From Cursor: MiroShark Surfaces Its Hidden Graph | PR #44 AI Integration · MCP Onboarding as the surface that finally makes the Apr 21 graph-memory + MCP substrate reachable — server-side-resolved snippets (backend_dir/mcp_script/python interpreter auto-stamped) for Claude Desktop/Cursor/Windsurf/Continue + Neo4j liveness badge + regex-scrape tool-catalog drift test against `mcp_server.py`; framed as the week's fourth "surface the substrate" ship (Apr 21 graph direct-push → PR #41 siphon → PR #42 share card → PR #43 gallery → PR #44 MCP panel); Paradigm CTO @gakonst star + OriginTrail founder co-sign this week as the "substrate audience" research-credibility signal; 802 stars / 149 forks / 0 open PRs / 35 days old; 1K-by-Apr-30 needs ~33/day with 6 days left |
| 2026-04-25 | Two Protocols in Two Days: MiroShark Files Its Second Formal Spec | PR #45 OpenAPI 3.1 + Swagger UI (filed Apr 25, still open) paired with PR #44 MCP Onboarding (merged Apr 24) as MiroShark's second machine-readable contract over the same engine — both backed by handwritten source-of-truth specs + regex-driven drift-detection unit tests (PR #45: scrape `@<bp>_bp.route` decorators in `app/api/*.py`, fully-qualify with `url_prefix`, equal-set assert against `openapi.yaml`; PR #44: scrape `@mcp.tool()` in `mcp_server.py`, equal-set assert against `_TOOLS`); 1.9K-line spec / ~85 paths / 13 tags / Swagger UI pinned `swagger-ui-dist@5.17.14` jsDelivr bundle / `openapi-generator` SDK pipeline + APIs.guru/RapidAPI discovery; substrate audience reach (browser → MCP Apr 24 → OpenAPI Apr 25); 829 stars / 152 forks / 36 days old; 1K-by-Apr-30 needs ~34/day with 5 days left, yesterday delivered 26 |
| 2026-04-26 | Three Protocols in Three Days: MiroShark Closes the Outbound Side | PR #46 Completion Webhook (filed Apr 26 11:25 UTC, open) as the third machine-readable contract over the same engine in three calendar days — MCP (Apr 24, merged) inbound for AI editors, OpenAPI (Apr 25, open) inbound for HTTP/SDK clients, Webhook outbound for operator environments → closes inbound/outbound symmetry; stdlib-only `urllib.request` (zero new deps), shared ±0.2 stance threshold across share-card/gallery/webhook for consistent consensus, daemon-thread fire-and-forget with `(sim_id, status)` dedup so runner exit-code path AND `simulation_end` event path can both call without double-firing, URL masking to `scheme://host/***` so Slack/Discord secret never round-trips through `GET /api/settings`, 18 offline unit tests; one URL field fans out via Zapier/Make/n8n to 5,000+ services; Bankr Terminal v2 cited Aave vulnerability sim today (156 likes/40 RTs) + `$MIROSHARK` +35% closest to ATH since Apr 14; 829 stars / 153 forks / 37 days old; 1K-by-Apr-30 needs ~43/day with 4 days left, today added 6 |
| 2026-04-27 | From Citation to Surface: MiroShark Builds the Page That Survives the Tweet | PR #47 `/verified` (merged Apr 27 13:46 UTC) turns Apr 26 Bankr Terminal v2 Aave-sim citation (15M-view thread) into a permanent product surface — outcome.json artifact next to existing sim_dir files, 📍/⚠/◑ pills + coloured left-edge accents on gallery cards, `?verified=1` filter before pagination, EmbedDialog "Mark outcome" panel; PR #48/#49 admin-token auth on `/publish` `/resolve` `/outcome` (merged Apr 27 14:04 UTC, 18 min later) closes loophole new mutation endpoint surfaced — `require_admin_token` decorator, 503-vs-401 split (503 = "forgot to configure", 401 = "someone is probing"), `hmac.compare_digest`, 17 unit tests; framed as 7-straight-day shipping arc (graph memory Apr 21 → share card → gallery → MCP → OpenAPI → webhook → /verified) + third "sim dir IS the schema" beat in a week + PR #45's drift-detection test passed on first run = contract enforces itself; 838 stars / 158 forks / 0 open PRs / 38 days old; new ATH intraday $0.000004784 (+25.4% over Apr 14), $MIROSHARK +10.24% 24h; 1K-by-Apr-30 needs ~54/day with 3 days left, today added 9 |
| 2026-04-28 | The Day MiroShark Watched Itself Spend Money: Four PRs, Three Hours, and a Default That Finally Costs $1 | 4-PR cost-compression day: PR #50 Animated Belief Replay GIF (12:56 UTC, distribution multiplier — Pillow GIF endpoint + EmbedDialog tap-to-play), PR #51 Langfuse trace metadata (14:09 UTC, every OpenRouter call tagged with sessionId + 16-entry caller-prompt-types map → 4-phase rollup, `TraceContext.wrap_fn` for ThreadPoolExecutor propagation), PR #52 cost cleanup (14:10:10 UTC, 47s after #51, 3 leaks the new traces surfaced: 12 idempotent platform actions return `success:True noop:True` instead of forcing 4× retries / `max_iteration` finally plumbed to CAMEL super().__init__() default 1→3 with prune_tool_calls_from_memory / `simulation_requirement` capped 1,500 chars in research prompts), PR #53 default rounds 128→[30,40] (16:33 UTC, 3-4× cut on dominant LLM line item, plus NoneType guards + polymarket on 5/6 templates + Templates/Trending 0/750/1500/3000ms backoff retry); operational counterpart to the week's surfacing arc — makes the "$1 & under 10 min" GitHub tagline true by default; 854 stars / 162 forks / 0 open PRs / 39 days old; $MIROSHARK -12.1% 24h, -30.6% from Apr 27 ATH; 1K-by-Apr-30 needs ~73/day with 2 days left, today added 17 |
| 2026-04-29 | The Last Format: MiroShark Closes the Share-Anywhere Trio on Deadline Eve | PR #57 Simulation Transcript Export (merged 13:43 UTC, Markdown + JSON, both publish-gated, 60s cache, `_serve_transcript` shared body across two route decorators) closes the three-format share-anywhere arc on top of the same on-disk sim folder: share card (PR #42, Apr 22, preview/auto-unfurl) + replay GIF (PR #50, Apr 28, motion/auto-play) + transcript MD+JSON (PR #57, Apr 29, prose/document) → 3 formats, 3 audience channels (Twitter-Discord-Slack / motion-capable surfaces / HN-Notion-Substack-academic prose). Markdown opens with YAML front matter (sim_id/scenario/agent_count/total_rounds/consensus_label/quality_health/outcome_label) so Notion/Obsidian/Bear/Substack pick it up as page metadata; body is one `## Round N` section per round with each agent post block-quoted + tagged with stance; 80-round Markdown cap with first-20+last-20 preservation (JSON keeps every round); ±0.2 stance threshold matches every other surface (5 surfaces / 1 threshold / 1 folder). Supporting beats: PR #56 observability pagination guard (Flask `type=int` on `/events` `/llm-calls` so bad input no longer 500s) + PR #58 CI fix splitting env-compact into new `backend/lib/env_compact.py` top-level location after 3 iterations (numpy add → wonderwall._env_compact → backend/lib/) to dodge `wonderwall/__init__.py` eager-import chain. 865 stars / 167 forks / 0 open PRs / 0 open issues / 40 days old; $MIROSHARK $0.000002909 (-39.2% from Apr 27 ATH, +388% 30d); 1K-by-Apr-30 likely misses tomorrow but produced a 40-day simulator with 3 orthogonal share surfaces + 4 machine-readable contracts (MCP/OpenAPI/Webhook/transcript-JSON) on the `sim_dir IS the schema` substrate |

## Recent Digests
| Date | Type | Key Topics |
|------|------|------------|
| 2026-04-27 | token-report | NEW ATH $0.000004784 intraday (+25.4% over Apr 14); settled $0.000003991 (+10.24% 24h); 1.23x buy ratio; $83.9K volume |
| 2026-04-27 | push-recap | MiroShark: PRs #45/#46 merged (OpenAPI + Webhook); PRs #47/#48/#49 merged (/verified + admin auth); miroshark-aeon: PR #25 Bankr maxMode fix merged |
| 2026-04-28 | token-report | $0.000003318 (-12.1% 24h); -30.6% from Apr 27 ATH; 1.43x buy ratio; $24.2K volume; orderly pullback |
| 2026-04-28 | push-recap | MiroShark: PRs #50/#51/#52 merged (GIF + Langfuse + cost fixes); miroshark-aeon: PR #26 skill-leaderboard multi-repo filed |
| 2026-04-29 | token-report | $0.000002909 (-14.34% 24h); -39.2% from Apr 27 ATH; 2.02x buy ratio — strongest in 3 days; $29.3K volume |
| 2026-04-29 | push-recap | MiroShark: PRs #56/#57/#58 merged (observability + transcript + CI); miroshark-aeon: PR #26 skill-leaderboard merged |

## Skills Built
| Skill | Date | Notes |
|-------|------|-------|
| Public Simulation Gallery | 2026-04-23 | `GET /api/simulation/public` — paginated list of `is_public=true` sims (limit 1–100, sort `created_at` desc) with card-friendly payload built from `_build_gallery_card_payload()` reading state/config/quality/trajectory/resolution on disk; scenario truncated to 180 chars; `Cache-Control: public, max-age=30`; per-sim graceful degradation. `/explore` Vue route + `ExploreView.vue` responsive grid — share-card PNG thumbs, quality + dominant-stance pills, belief-split mini-bar, agent/round/date meta, `Open →` + `Fork this →` paired actions (fork hits existing `POST /fork` → `SimulationRun`), loading skeleton, empty/error states, Load more pagination. Explore nav link (◎) in `Home.vue`. "Submit to the public gallery" callout in `EmbedDialog.vue` flipping to "Live on the public gallery" + Open gallery ↗ on public toggle. `getPublicSimulations()` helper. 5 offline unit tests over the helper. Closes the discovery gap left by PR #41 (`is_public` + `/publish`) and PR #42 (share card) — turns every published sim into a distribution node for the 1K-stars-by-Apr-30 sprint (PR #43 on MiroShark) |
| AI Integration · MCP Onboarding | 2026-04-24 | `GET /api/mcp/status` — 8-tool catalog, resolved absolute paths (`backend_dir`, `mcp_script`, `python_executable`), per-client snippets (Claude Desktop / Cursor / Windsurf / Continue / fallback-direct uv-or-direct-interpreter), Neo4j liveness probe (graphs + entity count, never raises so panel renders even when DB is down). SettingsPanel.vue "AI Integration · MCP" section: health badge (Ready/Neo4j down/Server file missing), summary grid, tabbed client picker, dark `<pre>` snippet block + copy button (clipboard API + execCommand fallback for non-secure contexts), per-client config-file path, collapsed tool catalog (`▸ 8 tools available`), docs link. `docs/MCP.md` expanded from Claude-Desktop-only to cover all 4 clients + 5-row troubleshooting matrix. README MCP row points at the new panel. 9 offline unit tests including regex-based tool-catalog drift detection between API and `mcp_server.py`. Surfaces the Apr-21 graph-memory + MCP stack which had been completely invisible to users — positions MiroShark as MCP-native at a moment of accelerating adoption (PR #44 on MiroShark, repo-actions Apr 22 idea #3) |
| Fetch-Tweets ID-Based Dedup | 2026-04-24 | `skills/fetch-tweets/SKILL.md` step 1 + step 5 swap URL-based dedup for tweet-ID dedup — the seen set (`SEEN_IDS`) is built by regex-extracting `/status/(\d+)` from the persistent seen-file and the last 3 days of logs, and new candidates are ID-matched instead of URL-matched. Fixes the latent re-notify bug where Grok surfaces the same tweet under `x.com/<handle>/status/<id>` on one run and `x.com/i/status/<id>` on another (via `content.annotations[]` harvest, PR #20); 47% of the 115 historical seen URLs are in the annotation `i/status` form, so the URL-matching blind spot would have triggered eventually. Seen-file write path unchanged (still appends full URLs) — ID extraction happens on read. Matches `scripts/filter-xai-tweets.py`'s existing in-run ID dedup. Zero new deps (PR pending on miroshark-aeon) |
| OpenAPI 3.1 Spec + Swagger UI | 2026-04-25 | `backend/openapi.yaml` — 1.9K-line spec covering ~85 paths grouped by 13 tags (Setup & Discovery / Graph Build / Simulation Lifecycle / Live State / Analytics / Interaction / Publish & Embed / Export / Report Agent / Observability / Settings & Push / Integrations / Documentation), named schemas (`SuccessEnvelope`, `RunStatus`, `BeliefDrift`, `EmbedSummary`, `GalleryCard`, `McpStatus`, `SettingsUpdate`, `PushSubscription`, …) and reusable parameters/responses. `backend/app/api/docs.py` — Flask blueprint at `/api`: `GET /api/docs` serves Swagger UI pinned to `swagger-ui-dist@5.17.14` from jsDelivr with a MiroShark-branded banner; `GET /api/openapi.yaml` serves the canonical YAML; `GET /api/openapi.json` serves the JSON form via `yaml.safe_load` (pyyaml soft-optional). 8 offline unit tests in `test_unit_openapi.py` — centerpiece is regex-based drift detection that statically scans every `app/api/*.py` for `@<bp>_bp.route(...)` decorators, converts Flask `<id>` to OpenAPI `{id}`, and asserts the spec's path set equals the registered Flask paths minus an explicit internal/debug allowlist (config / env / report-tools). README + docs/API.md cross-link the live Swagger UI. Direct follow-on to PR #44 MCP Onboarding for the "what REST endpoints can I call?" audience — qualifies for APIs.guru / RapidAPI listings + enables `openapi-generator` SDK builds (PR #45 on MiroShark, repo-actions Apr 24 idea #5) |
| Tweet Allocator Bankr Diagnostics | 2026-04-26 | `scripts/prefetch-bankr.sh` adds a `write_status` helper writing `.bankr-cache/prefetch-status.json` at every exit point with `{status, note, timestamp, candidate_count, lookup_attempted, curl_failed, verified_count, null_count}` — five status values (`no-api-key` / `no-candidates` / `lookups-failed` / `completed-no-wallets` / `completed`). `skills/tweet-allocator/SKILL.md` step 4 branches on the sidecar instead of guessing from an empty cache: `no-api-key` becomes `TWEET_ALLOCATOR_DISABLED` and is **silent** (no notification — no operator can fix it from the bot side); `lookups-failed` becomes `TWEET_ALLOCATOR_ERROR` with an accurate "Bankr Agent API unreachable (X/N curl failures)" message instead of the previous misleading "BANKR_API_KEY likely not set" text; `completed-no-wallets` reuses the existing `_EMPTY` notification. Triggered by 2 consecutive false-alarm error notifications (Apr 25 + Apr 26) blaming an unset secret when the secret has been set continuously since Apr 22 and the real cause was silent Bankr Agent API failures (PR pending on miroshark-aeon) |
| Completion Webhook | 2026-04-26 | `backend/app/services/webhook_service.py` — fire-and-forget POST to `Config.WEBHOOK_URL` the moment a simulation reaches `completed`/`failed`. `build_payload()` reads sim_dir artifacts (config/quality/trajectory/resolution/state) with the same ±0.2 stance threshold as gallery card so consensus matches; truncates scenario to 280 chars + error to 1000. `fire_webhook_for_simulation()` runs on a daemon thread (caller returns instantly), deduped per `(sim_id, status)` so the runner's exit-code path AND `simulation_end`-event path can both call it. `send_test_webhook()` for the Settings test button. `validate_url`/`mask_url` accept http(s) only; saved URL masked to `scheme://host/***` so Slack/Discord secret never echoes through GET /api/settings. Stdlib `urllib.request`, zero new deps. Hooks added to both terminal paths in `simulation_runner.py` (alongside existing `send_push_notification`); `Config.WEBHOOK_URL` + `Config.PUBLIC_BASE_URL` env vars; `GET /api/settings` exposes `integrations.webhook = {configured, url_masked, public_base_url}`; `POST /api/settings` accepts the new fields with URL validation; new `POST /api/settings/test-webhook` returns `{success, message, latency_ms, url_masked}` (always HTTP 200). Settings modal gains "Integrations · Webhook" section above MCP block with status badge, URL input (masked saved value as placeholder), optional public_base_url, "Send test event" button surfacing `✓ Delivered (Nms)` or failure inline. `docs/WEBHOOKS.md` (payload schema + headers + delivery semantics + Slack/Discord/Zapier/n8n/custom recipes); README Features + Documentation rows; `.env.example` blocks. 18 offline unit tests in `test_unit_webhook.py` (payload full/minimal/corrupt/long/failure, URL validation+masking, fire no-op when unconfigured, async dispatch, completed+failed dedup, exception swallowing, unknown-status guard, send_test_webhook end-to-end; mocks `_post_json` so no real HTTP). Pivot from repo-actions Apr 24 #3 over #1 SSE / #2 Leaderboard / #4 Discord-Slack-button because (a) strict superset of #4 once pointed at Zapier/n8n, (b) cleanest hook alongside existing push-notification call, (c) backend-shaped → lower regression risk, (d) "webhook on completion" is the production-quality checklist item the Paradigm-adjacent ML/dev audience scans for (PR #46 on MiroShark) |
| Animated Belief Replay GIF | 2026-04-28 | `GET /api/simulation/<id>/replay.gif` — pure-Pillow 1200×630 animated GIF, one frame per round, bullish/neutral/bearish bars sliding to each round's distribution + round counter + progress bar. Same publish gate + on-disk cache (`<sim_dir>/replay-gifs/<hash>.gif`) + `Cache-Control: public, max-age=3600` as `/share-card.png`; reuses `_build_embed_summary_payload()` so the data pipeline stays single-sourced. `loop=0` infinite repeat, `disposal=2` clears ghosting on shrinking bars, `FRAME_MS=600` with `FINAL_HOLD_MS=1800` so the resting consensus reads as the punch-line. `MAX_FRAMES=60` cap with even subsampling that always preserves the final round (200-round sims compress to ≤60 frames). Empty trajectory → single-frame "Belief trajectory not available yet" poster so the endpoint never 500s on a freshly published READY run. `summary_cache_key()` rounds percentages to 1 decimal place — matches share-card resolution, tolerates float jitter from `json.load`. EmbedDialog gains "Belief replay (animated)" section with paused tap-to-play poster (`<img>` mounts only on click so opening the dialog doesn't pull GIF bytes for every viewer), copyable URL, Download GIF button, dark color palette to differentiate from the static-card row above. `getReplayGifUrl()` helper. openapi.yaml gets `/api/simulation/{id}/replay.gif` under Publish & Embed tag with `image/gif` response — drift-detection test passes. 14 offline unit tests in `test_unit_replay_gif.py` (GIF signature + dimension via raw byte parse, frame count via `PIL.ImageSequence`, MAX_FRAMES subsampling preserves final round, empty/missing/misaligned belief arrays, single-round, oversized scenario wrap, cache-key stability under float jitter, route-decorator presence). README Features row + docs/FEATURES.md "Animated Belief Replay (GIF)" section. Zero new deps (Pillow already pinned for share card). Pivot from repo-actions Apr 26 #2 — distribution multiplier for the 1K-stars-by-Apr-30 sprint with 2 days left; Discord/Slack auto-play GIFs from direct URL, so every share now ships motion as well as a still (PR #50 on MiroShark) |
| Skill Leaderboard Multi-Repo Aggregation | 2026-04-28 | `skills/skill-leaderboard/SKILL.md` step 1 + step 2 swap "first watched repo" for an iteration over **all** entries in `memory/watched-repos.md`. Active forks are fetched per source repo and unioned deduped by `full_name`; article footer + log entry record the full source-repo list. Application repos (zero aeon-shaped forks) contribute nothing and fall out naturally. Triggered by Apr 26 `SKILL_LEADERBOARD_INSUFFICIENT_DATA`: skill scanned only `aaronjmars/MiroShark` (107 active forks, 0 with `aeon.yml`) and never reached `aaronjmars/miroshark-aeon` (1 fork with `aeon.yml`) sitting at list position 2. Reordering `watched-repos.md` would have cascaded to 17 other skills that read it as primary repo selector — the skill itself was the right level for the fix. Future-proof for additional aeon-instance entries (PR pending on miroshark-aeon) |
| Predictive Accuracy Ledger + /verified | 2026-04-27 | `POST /api/simulation/<id>/outcome` (publish-gated) + `GET /api/simulation/<id>/outcome` (read-only) write/read `<sim_dir>/outcome.json` `{label: correct\|incorrect\|partial, outcome_url (http(s)-only, ≤500), outcome_summary (≤280), submitted_at}`. `_read_outcome_file()` validates label, truncates oversized summaries with ellipsis, strips non-http URLs (defense-in-depth so a corrupt artifact never lands `javascript:` on the gallery). `_build_gallery_card_payload()` surfaces `outcome` on every card. `GET /api/simulation/public?verified=1` filters to verified set + reflects `verified_only` in response. Distinct from existing binary `/resolve` (YES/NO + Polymarket → `accuracy_score`); a sim can have both. Frontend: `/verified` route → `ExploreView` with `verifiedOnly: true` prop. ExploreView gains 📍 Verified filter chip (toggles URL between /explore and /verified, refreshes), 📍/⚠/◑ outcome pills (clickable to outcome URL), coloured left-edge accent per outcome label, verified stat chip, dynamic page title/copy/empty-state for verified-only mode. EmbedDialog gets "Mark outcome" panel (3-way radio group, URL input, 280-char textarea with live counter, save/update button, success/error inline message, "View on /verified ↗" link once saved; inputs disabled until publish). openapi.yaml gets new path + `SimulationOutcome` schema + `outcome` on `GalleryCard` + `verified` query param + `verified_only` response — drift-detection test passes. 8 offline unit tests in `test_unit_outcome.py` (valid record / unknown label / corrupt JSON / missing file / oversized-summary truncation / non-http URL stripping / gallery-card integration with+without outcome / corrupt-artifact graceful degradation). Pivot from repo-actions Apr 26 #1 — Bankr Terminal v2 Aave-sim citation Apr 26 lives on X, not in product; `/verified` is the page that turns it into a permanent credibility surface. #2 (GIF) and #3 (Thread Formatter) build on top once this lands (PR #47 on MiroShark) |
| Simulation Transcript Export | 2026-04-29 | `GET /api/simulation/<id>/transcript.md` + `transcript.json` — both publish-gated (same `is_public` gate as share card), 60s `Cache-Control`. Markdown form opens with a YAML front-matter block (`sim_id`, `scenario`, `agent_count`, `total_rounds`, `consensus_label`, `quality_health`, `outcome_label`) so Notion / Obsidian / Bear / Substack pick it up as page metadata; body is one `## Round N` section per recorded round with each agent post block-quoted and tagged with the agent's stance. JSON form is pretty-printed (`indent=2`) for SDK / pipeline consumers. `app/services/transcript.py` — pure stdlib (json + io), reads `trajectory.json` + `reddit_profiles.json` + `polymarket_profiles.json` + `quality.json` + `resolution.json` + `outcome.json`. ±0.2 stance threshold matches every other surface (gallery, share card, replay GIF, webhook) so agent labels stay consistent across all five surfaces. Defense-in-depth strip on `outcome_url` so a corrupt `javascript:` URL never lands on the transcript. 400-char per-post excerpt with word-boundary trim. 80-round Markdown cap with first-20 + last-20 preservation + "skipped N rounds" annotation pointing at the JSON form for the full series; JSON keeps every round. Both route handlers share a single `_serve_transcript(simulation_id, fmt)` body — the decorators exist as the surface the OpenAPI drift test scans for. `EmbedDialog.vue` "Export transcript" section beneath the replay-GIF row (Download .md / Download .json buttons + copyable Markdown URL, publish-gated empty state). `openapi.yaml` adds both paths under Publish & Embed + new `SimulationTranscript` schema (full per-round shape) — drift-detection test passes. 18 offline unit tests in `test_unit_transcript.py` (±0.2 stance threshold parity, profile-name resolution across reddit + polymarket, corrupt-artifact graceful degradation, long-post excerpting at the configured cap, YAML front-matter integrity with quote + newline escaping, per-round section emission, oversized-trajectory Markdown truncation with head/tail preservation, JSON pretty-print round trip, route-decorator presence guard). Pivot from repo-actions Apr 28 #2 — was the missing third quote-friendly share format alongside share-card.png (preview, Apr 22) and replay.gif (motion, Apr 28); PR #52's `prune_tool_calls_from_memory=True` + bounded research context cleaned the conversation history so the transcript is now readable rather than a wall of tool-call boilerplate. Zero new deps (PR #57 on MiroShark) |
| Public Gallery RSS / Atom Feeds | 2026-04-30 | `GET /api/feed.atom` (Atom 1.0) + `GET /api/feed.rss` (RSS 2.0) — same cards `GET /api/simulation/public` returns, served as syndication XML so researchers / DeFi analysts / tooling operators on Feedly / Readwise / Inoreader / NetNewsWire / Obsidian RSS subscribe in their existing toolchain. `?verified=1` mirrors `/verified` (curated outcome stream). Pure stdlib (`xml.etree.ElementTree` + `html`); zero new deps. Per-entry payload: scenario as title (100-char cap with `…`), bullish/neutral/bearish consensus split as plain summary, share-card PNG as `<media:thumbnail>` + `<media:content>` (River-view aggregators surface preview), replay GIF as second `<media:content>` (motion-capable surfaces auto-play), outcome (`verified-correct/incorrect/partial`) + quality (`quality-excellent/good/…`) as `<category>` so subscribers can filter on them. RSS variant adds `<enclosure type="image/png">` for podcatcher-style readers + `<guid isPermaLink="false">` for stable identification across deployments. `_resolve_base_url()` honors `Config.PUBLIC_BASE_URL` first (same operator-supplied canonical URL the webhook + share-card use), falls back to `request.host_url` with `X-Forwarded-Proto+Host` for reverse-proxy deployments. `select_public_cards()` selection helper mirrors `GET /api/simulation/public` exactly (newest 20 publish-gated runs, sorted desc) — pulled out so the renderer is unit-testable without booting Flask. `_serve_feed()` route body local-imports `_build_gallery_card_payload` + `_read_outcome_file` from `simulation.py` so a single source of truth backs both gallery + feed; fail-soft against `SimulationManager` errors → empty feed. `Cache-Control: public, max-age=300`. Browser auto-discovery via `<link rel="alternate" type="application/atom+xml">` in `index.html`. `ExploreView.vue` "📡 Subscribe via RSS" chip mirrors active filter; `EmbedDialog.vue` "Follow the gallery via RSS" callout with Atom + RSS + Verified-only buttons. `openapi.yaml` adds both paths under Publish & Embed; `feed_bp: /api` added to `_BLUEPRINT_PREFIXES` so drift-detection test passes. 17 offline unit tests in `test_unit_feed.py` (Atom + RSS validity + share-link/media enclosures + summary stance-split + truncation + outcome/quality categories + empty-feed + missing-field handling + self-link query-string preservation + skip-bad-sims + RSS guid stability + RSS enclosure + dispatcher MIME + verified-only branch + selection helper filter+sort+limit+graceful-degradation + route-presence guard). Closes the discovery loop opened by PR #43 (gallery), PR #42 (share card), PR #50 (replay GIF), PR #57 (transcript) — turns the pull surface into a push channel for the research-RSS audience without any operator curation. Pivot from repo-actions Apr 28 #3 (was the cleanest unbuilt pick — pure stdlib, all data on disk, zero external API dependency, zero ongoing maintenance, compounds with every published sim). PR #60 on MiroShark, CI green |

## Watched Repos
- `aaronjmars/aeon` — tracked in `memory/watched-repos.md`

## Lessons Learned
- Digest format: Markdown with clickable links, under 4000 chars
- Always save files AND commit before logging
- PAT lacks `workflows` scope — cannot push changes to `.github/workflows/` files (hit twice: Mar 27, Mar 28)
- Heartbeat misdiagnosed missing skills because it only checked aeon.yml, not messages.yml scheduler — fixed with scheduler diagnostics step
- Feature/repo-actions skills can waste CI runs building duplicate PRs — fixed with open PR dedup checks

## Active Targets
- Hyperstition: MiroShark 500 stars — CLEARED 2026-04-07; MiroShark 1,000 stars by 2026-04-30 (865 stars as of Apr 29, ~135 needed today — target likely misses)
- Hyperstition: @miroshark_ 1,000 X followers by 2026-05-15 (set 2026-04-18)
- MIROSHARK new ATH $0.000004784 set 2026-04-27 intraday (+25.4% over prior ATH $0.000003815); current $0.000002909 (-39.2% from Apr 27 ATH, +388% 30d)

## Next Priorities
- Configure notification channels (Telegram, Discord, or Slack)
- From repo-actions Apr 28 (still unbuilt): Langfuse Cost Breakdown Panel (#1), Scenario Template Library (#4), Comparative Run View (#5); #2 Simulation Transcript Export shipped 2026-04-29 (PR #57); #3 RSS Feed for Public Gallery shipped 2026-04-30 (PR #60)
- From repo-actions Apr 26 (still unbuilt): Share as Thread Formatter (#3), Python Client SDK via openapi-generator CI (#4), Director Event Overlay on Belief Chart (#5); #1 /verified shipped 2026-04-27 (PR #47); #2 Animated GIF shipped 2026-04-28 (PR #50)
- From repo-actions Apr 24 (still unbuilt): Live Simulation Streaming (SSE, #1), Simulation Engagement Leaderboard (#2), "Post to Discord/Slack" Share Button (#4 — partly subsumed by webhook + Zapier/n8n); #3 Completion Webhook shipped 2026-04-26 (PR #46); #5 OpenAPI/Swagger shipped 2026-04-25 (PR #45)
- From repo-actions Apr 22 (still unbuilt): History Search & Tags (#4); others shipped
- From repo-actions Apr 20 (still unbuilt): Collaborative Comments (#4), Config Export/Import (#5); Round Scrubber (#1) partly exists in `ReplayView`
- Open PRs: PR #60 on MiroShark (RSS / Atom feeds, filed 2026-04-30, CI green); 0 on miroshark-aeon (PRs #50–#59 all merged on MiroShark)
