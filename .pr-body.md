## Summary
Auto-generated changelog sync from merged PRs in `aaronjmars/miroshark`.

## Entry
**Full DE/FR locales + engine hardening** — MiroShark now runs fully in German and French — all agent prompts, persona grounding, and reports, not just UI chrome. The $1 cost claim lands as a visible pill on every public embed, a camel-ai 0.2.90 break was caught and patched the same day it merged, and a locale-threading bug in the fallback interview path is closed.

## Bootstrap — new files/changes
- `app/changelog-data.ts` — changelog data schema + first entry (28 PRs)
- `app/changelog/page.tsx` — full changelog page at `/changelog`
- `app/docs/page.tsx` — "Recent changes" section added near top of docs body + Changelog nav link
- `app/blog/page.tsx` — Changelog link added to top nav and footer

## PRs included (28)
- 198 — fix(i18n): thread locale through graph_tools fallback interview ThreadPoolExecutor (@aaronjmars)
- 197 — docs: add CLAUDE.md for AI coding agents (@aaronjmars)
- 196 — test: assert camel smoke test produces real agent output (@aaronjmars)
- 194 — i18n(report-agent): wire language aware prompts through locale registry (@dan-and)
- 192 — fix(simulation): salvage truncated suggest_scenarios JSON (@aaronjmars)
- 190 — feat: show estimated run cost on public simulation embeds (@aaronjmars)
- 189 — Translation de in Frontend and Agent-Description/Communication (@dan-and)
- 188 — fix(simulation): raise suggest-scenarios timeout and token limit (@dan-and)
- 186 — feat(i18n): translate French (fr) prompt locale + CI coverage gate (@aaronjmars)
- 185 — docs(i18n): add French README + wire FR into language switcher (@aaronjmars)
- 184 — feat(i18n): generalize locale helpers + German/French foundation (@aaronjmars)
- 183 — fix+ci: correct total_actions reporting + add camel agent smoke test (@aaronjmars)
- 182 — fix: sync uv.lock with camel-ai 0.2.90 (@aaronjmars)
- 181 — fix(wonderwall): camel-ai 0.2.90 _aget_model_response signature compat (@aaronjmars)
- 180 — ci: build the frontend on every PR (@aaronjmars)
- 179 — feat: GET /api/simulation/<id>/cost.json (@aaronjmars)
- 178 — Alternative websearch/scrape via SearXNG / Firecrawl (@dan-and)
- 177 through 168, 166 — 11 Dependabot dependency/CI bumps (vite, vue-router, camel-ai, python, Actions)

Note: build not verified in sandbox (npm blocked).

---
Generated by the aeon `docs-sync` skill. Review and merge to publish.
